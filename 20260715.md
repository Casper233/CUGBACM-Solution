# CUGBACM25级暑假小学期训练 #9 数论

## 一、数论基础

### 1.1 最大公约数(GCD)

最⼤公约数，Greatest Common Divisor（GCD），指两个整数的所有公共约数中最大的数。记作\(gcd(a, b)\)。~~(wow latex真好用)~~
$$
gcd(a, b) = max\{d: \ d|a \ 且\ d|b\}
$$

最大公约数运算有以下几个性质：

- 交换律：
  $$
  gcd(a, b) = gcd(b, a)
  $$

- 加减不变：
  $$
  gcd(a, b) = gcd(a, a ± b)
  $$

- 倍数运算：
  $$
  gcd(ma, mb) = m × gcd(a, b)
  $$

- 模运算性质：
  $$
  gcd(a, b) = gcd(b, a \% b)
  $$

> [!NOTE]
>
> ### 模运算性质证明如下：
>
> 设 \(a, b\) 为整数，且 \(b > 0\)，令 \(r = a \bmod b\)（即 \(a\) 除以 \(b\) 的余数），则：
> $$
> \gcd(a, b) = \gcd(b, r)
> $$
>
> ---
>
> **余数为 0时：**
> 如果 \(r = 0\)，说明 \(b\) 整除 \(a\)。
> 此时 \(\gcd(a, b) = b\)，而 \(\gcd(b, 0) = b\)（因为任何非零整数整除 0，且最大公约数为 \(b\) 本身）。
> 等式显然成立。下面我们假设 \(r > 0\)。
>
> ---
>
> **利用最大公约数进行变量替换：**
> 令
> $$
> k = \gcd(a, b)
> $$
> 因为 \(k\) 同时整除 \(a\) 和 \(b\)，所以存在互质的两个整数 \(x\) 和 \(y\)，使得：
> $$
> a = kx, \quad b = ky
> $$
> 并且满足：
> $$
> \gcd(x, y) = 1
> $$
>
> ---
>
> **用带余除法表示余数 \(r\)：**
> 设 \(m = \left\lfloor \frac{a}{b} \right\rfloor\)（即 \(a\) 除以 \(b\) 的整数商），根据带余除法的定义有：
> $$
> a = mb + r
> $$
> 将 \(a = kx\) 和 \(b = ky\) 代入上式，移项解出 \(r\)：
> $$
> r = a - mb = kx - m(ky) = k(x - my)
> $$
> 此时我们得到：
>
> - \(b = ky\)
> - \(r = k(x - my)\)
>
> 显然，\(k\) 是 \(b\) 和 \(r\) 的一个公因数。
>
> 证明 \(k\) 是最大公因数，我们只需要证明剩下的部分 \(y\) 与 \(x - my\) 互质。
>
> ---
>
> **证明 \(\gcd(y,\ x - my) = 1\)：**
> 不妨使用采用反证法：
>
> 假设 \(\gcd(y,\ x - my) = d > 1\)。
> 那么根据公约数的定义：
> $$
> d \mid y \quad \text{且} \quad d \mid (x - my)
> $$
>
> 因为 \(d \mid y\)，所以 \(d\) 也整除 \(y\) 的整数倍，即：
> $$
> d \mid my
> $$
>
> 又因为 \(d \mid (x - my)\)，根据整除的线性组合性质，\(d\) 必定整除它们的和：
> $$
> d \mid \big[(x - my) + my\big]
> $$
> 即：
> $$
> d \mid x
> $$
>
> 于是我们得出：\(d\) 同时整除 \(x\) 和 \(y\)。
> 所以 \(d\) 是 \(x\) 和 \(y\) 的一个公因数，那么它们的最大公约数至少为 \(d\)：
> $$
> \gcd(x, y) \ge d > 1
> $$
>
> 但这与第二步中已知的 \(\gcd(x, y) = 1\) 直接矛盾。
> 因此，假设不成立，只能有：
> $$
> \gcd(y,\ x - my) = 1
> $$
>
> ---
>
> **利用最大公约数的数乘性质得出结论：**
> 最大公约数有一个基本性质：对于任意整数 \(c\)，有 \(\gcd(kc, kd) = k \cdot \gcd(c, d)\)（即提取公因数）。
>
> 利用这个性质，计算 \(\gcd(b, r)\)：
> $$
> \gcd(b, r) = \gcd\big(ky,\ k(x - my)\big) = k \cdot \gcd\big(y,\ x - my\big)
> $$
>
> 将第四步证明的结果 \(\gcd(y,\ x - my) = 1\) 代入：
> $$
> \gcd(b, r) = k \cdot 1 = k
> $$
>
> 而最初我们设了 \(k = \gcd(a, b)\)，因此：
> $$
> \gcd(b, r) = \gcd(a, b)
> $$
>
> ---
>
> 综上，对于任意正整数 \(a, b\)（\(b \neq 0\)），令 \(r = a \bmod b\)，我们严谨地证明了：
> $$
> \boxed{\gcd(a, b) = \gcd(b, r)}
> $$
> 每一次用除数除以余数，最大公约数保持不变，直到余数为 0 时，除数即为答案。

辗转相除法模板如下：

```c++
long long gcd(long long a, long long b) {
    return b == 0 ? a : gcd(b, a % b);
}
```

C++ 中可直接调⽤ __gcd(a, b) ，C++17 中引入了 gcd(a, b) 函数（需包含<numeric>）

### 1.2 最小公倍数(LCM)

最⼩公倍数，Least Common Multiple（LCM），是指两个整数的公共倍数中最小的数，记作 \(lcm(a,b)\)。有：
$$
lcm(a, b) = min\{d:\ a|d \ 且\ b|d \}
$$
我们经过推导可以得到最大公约数推导最小公倍数的公式：
$$
\operatorname{lcm}(a,b)=\frac{a\cdot b}{\gcd(a,b)}
$$

> [!NOTE]
>
> 证明推导如下：
>
> 令：
> $$
> g = \gcd(a, b)
> $$
> 因为 \(g\) 同时整除 \(a\) 和 \(b\)，所以存在两个正整数 \(x, y\)，使得：
> $$
> a = gx, \quad b = gy
> $$
> 并且根据最大公约数的定义，提取出最大公因数后，剩下的两个数必然互质。
> $$
> \gcd(x, y) = 1
> $$
>
> ---
>
> **推导出最小公倍数的候选值：**
>
> 将 \(a = gx\)、\(b = gy\) 代入公式右边：
> $$
> \frac{a \cdot b}{g} = \frac{(gx) \cdot (gy)}{g} = gxy
> $$
> 因此，我们只需要证明：
> $$
> \operatorname{lcm}(a, b) = gxy
> $$
> 也就是说，证明 \(gxy\) 同时是 \(a\) 和 \(b\) 的公倍数，并且是所有公倍数中最小的一个。
>
> ---
>
> **证明 \(gxy\) 是公倍数：**
>
> 因为：
> $$
> gxy = (gx) \cdot y = a \cdot y
> $$
> 所以 \(gxy\) 能被 \(a\) 整除（即 \(a \mid gxy\)）。
>
> 同理：
> $$
> gxy = (gy) \cdot x = b \cdot x
> $$
> 所以 \(gxy\) 也能被 \(b\) 整除（即 \(b \mid gxy\)）。
>
> 因此，\(gxy\) 确实是 \(a\) 和 \(b\) 的一个公倍数。
>
> ---
>
> **证明 \(gxy\) 是所有公倍数中最小的：**
>
> 设 \(M\) 是 \(a\) 和 \(b\) 的任意一个公倍数，即：
> $$
> a \mid M \quad \text{且} \quad b \mid M
> $$
> 因为 \(a = gx\)，所以 \(gx \mid M\)，这意味着存在某个整数 \(m\)，使得：
> $$
> M = gx \cdot m \quad \text{(1)}
> $$
> 同理，因为 \(b = gy\)，所以 \(gy \mid M\)，这意味着存在某个整数 \(n\)，使得：
> $$
> M = gy \cdot n \quad \text{(2)}
> $$
>
> 由（1）和（2）相等，可得：
> $$
> gx \cdot m = gy \cdot n
> $$
> 两边同时除以 \(g\)（\(g>0\)），得到：
> $$
> x \cdot m = y \cdot n
> $$
> 也就是说：
> $$
> y \mid (x \cdot m)
> $$
>
> 因为 \(\gcd(x, y) = 1\)，且 \(y\) 整除 \(x \cdot m\)，根据欧几里得引理（若互质的数整除乘积，则它必然整除另一个因子），可得：
> $$
> y \mid m
> $$
> 于是存在整数 \(t\)，使得：
> $$
> m = y \cdot t
> $$
>
> 把 \(m = yt\) 代回（1）式：
> $$
> M = gx \cdot (yt) = (gxy) \cdot t
> $$
> 说明任意公倍数 \(M\) 都一定是 \(gxy\) 的整数倍。
>
> 既然 \(M\) 是 \(gxy\) 的正整数倍（倍数 \(t \ge 1\)），那么必然有：
> $$
> M \ge gxy
> $$
> 因为 \(M\) 是任取的，所以没有任何公倍数能小于 \(gxy\)。因此，\(gxy\) 就是最小公倍数。
>
> 即得到下式：
> $$
> \operatorname{lcm}(a, b) = gxy = \frac{a \cdot b}{g} = \frac{a \cdot b}{\gcd(a, b)}
> $$

最大公约数提取出来后，剩余部分互质，利用互质的性质（欧几里得引理）推出任意公倍数都必须是该候选值的倍数，得到最小公倍数。

为防止由于数组超范围，在实现时先除再乘。

最小公倍数的模板如下：

```c++
long long lcm(long long a, long long b) {
    return a / gcd(a, b) * b;
}
```

C++17 标准⽀持 lcm(a, b) 函数（需包含 <numeric>）。

#### 题目1：

> ## 题目描述
>
> 输入两个正整数 x0,y0，求出满足下列条件的 P,Q 的个数：
>
> 1. P,Q 是正整数。
> 2. 要求 P,Q 以 x0 为最大公约数，以 y0 为最小公倍数。
>
> 试求：满足条件的所有可能的 P,Q 的个数。
>
> ## 输入格式
>
> 一行两个正整数 x0,y0。
>
> ## 输出格式
>
> 一行一个数，表示求出满足条件的 P,Q 的个数。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 3 60
> ```
>
> **输出 #1**
>
> ```
> 4
> ```
>
> ## 说明/提示
>
> P,Q 有 4 种：
>
> 1. 3,60。
> 2. 15,12。
> 3. 12,15。
> 4. 60,3。
>
> 对于 100% 的数据，2≤x0,y0≤10^5。
>
> **【题目来源】**
>
> NOIP 2001 普及组第二题

本题我们发现P、Q题目说只有4种，两组。我们只需要判断其是否是满足首先两者\(gcd\)相同满足条件，当\(gcd\)相同时，\(lcm\)相同。在这里，因为数据量不大，我们甚至可以遍历整个\(x\),\(y\)范围。

已知公式：
$$
\gcd(i, j) \times \operatorname{lcm}(i, j) = i \times j
$$
所以当 \(\gcd(i, j) = x\) 且 \(\operatorname{lcm}(i, j) = y\) 时，必有：
$$
i \times j = x \times y
$$

我们遍历了所有 \(i\) 从 \(x\) 到 \(y\)，令另一个数 \(j = \frac{x \times y}{i}\)，然后检查：

- \(\frac{x \times y}{i}\) 是否是整数（即 \(x \times y\) 能被 \(i\) 整除）；
- \(\gcd(i, \frac{x \times y}{i})\) 是否等于 \(x\)。

如果两个条件都满足，则计数加一。

对于任意满足条件的 \(i\)，它必然在 \([x, y]\) 之间。

代码如下：

```c++
#include <iostream>
using namespace std;

long long gcd(long long a, long long b) {
    if (b == 0) return a;
    return gcd(b ,a % b);
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    long long x, y;
    int cnt = 0;
    cin >> x >> y;

    for (int i = x; i <= y; ++i) {
        if (x * y % i == 0 && gcd (i, x * y / i) == x) cnt ++;
    }
    
    cout << cnt;

    return 0;
}
```

---

## 二、素数筛法

也就是判断素数组的方法。

### 2.1 普通筛法

素数筛法是⽤于快速找出 1 ∼ n 范围内所有素数的算法，是数论中的基础⼯具。

对于每个整数\(x\)，将其所有倍数\(2x, 3x, 4x,...\)标记为合数。

模板如下：

```c++
bool vis[N]; //如果为1表示合数
int primes[N], cnt; //质数表和指数个数

void getprime(int n) {
    vis[1] = 1;
    for (int i = 2; i <= n; ++i) {
        if (!vis[i])
            prime[++cnt] = i;
        for (int j = 2 * i; j <= n; j = j + i) {
            vis[j] = 1
        }
    }
}
```

就这样，时间复杂度为\(O(n\ log\ n)\)。

### 2.2 埃氏筛法

我们发现，在普通筛法中，所有数均会被遍历一遍，并且会重复处理部分数值。

以``2, 4, 8``为例，在筛到``2``时，``4``与``8``都被标记为合数，但是我们遍历到``4``时，``4``会重复把``8``标记为合数。所以我们在for循环时可以给他提出，减小时间复杂度。

修改如下：

```c++
bool vis[N]; //如果为1表示合数
int primes[N], cnt; //质数表和指数个数

void getprime(int n) {
    vis[1] = 1;
    for (int i = 2; i <= n; ++i) {
        if (!vis[i]) {
            prime[++cnt] = i;
        	for (int j = 2 * i; j <= n; j = j + i) {
            	vis[j] = 1
            }
        }
    }
}
```

与此同时更进一步的，我们在筛``5``时，我们发现``5 * 1``,``5 * 2``，``5 * 3``，``5 * 4``，一定都已经被筛过了，直接从 \(i^2\) 来开始进行下一次循环即可。

即有：

```c++
bool vis[N]; //如果为1表示合数
int primes[N], cnt; //质数表和指数个数

void getprime(int n) {
    vis[1] = 1;
    for (int i = 2; i <= n; ++i) {
        if (!vis[i]) {
            prime[++cnt] = i;
        	for (int j = i * i; j <= n; j = j + i) {
            	vis[j] = 1
            }
        }
    }
}
```

时间复杂度为\(O(n \ log \ n \ log \ n )\)。

### 2.3 欧拉筛

但是我们发现，以``2, 3, 4, 6, 12``为例，``12``会被筛2次，一次在处理``2``时，一次在处理``3``时。人们对于埃氏筛的时间复杂度还是不满意，提出了一个线性的时间复杂度的解法。

欧拉筛，也称为线性筛。

让每个合数只被它的最小质因数筛掉，这样每个数只会被筛⼀次，达到真正的线性复杂度。原理是我们可以将一个数给他分解成素数乘积，\(12 = 2 × 2 × 3\)，详细见下一节。

即有：

```c++
bool vis[N]; //如果为1表示合数
int primes[N], cnt; //质数表和指数个数

void getprime(int n) {
   for (int i = 2; i <= n; ++i) {
       if (!vis[i])
           primes[++cnt] = i;
       for (int j = 1; j <= tot && i * primes[j] <= n; j++) {
           vis[i * primes[j]] = 1;
           if (i % primes[j] == 0)
               break;
       }
   }
}
```

当 \(i\  \% \ primes[j]\) == 0 时：

- 说明 \(primes[j]\) 是 (\i\) 的最小质因数

- 那么 \(i × primes[k]\ (k > j) \)的最小质因数⼀定也是 \(primes[j]\)

这些数会在后续被 primes[j] 筛掉，现在不需要重复筛。

时间复杂度为\(O(n)\)，是真正的线性。

#### 题目2：

> ## 题目背景
>
> 本题已更新，从判断素数改为了查询第 k 小的素数。
>
> 提示：本题输入输出、运算数据量较大。
>
> - 对于 C++ 语言，如果你使用 `cin` 来输入输出，建议使用 `std::ios::sync_with_stdio(0)` 来加速，同时使用 `'\n'` 换行输出。
> - 对于 Java 语言，使用线性筛并且优化输入输出，也可以在规定时限内通过本题，但是时限可能较紧张。
> - 对于 Python 语言，语言性能差异较大，需要使用到 `numpy` 库的数组以替代列表，且使用埃氏筛法，依然可以在合适的时间和内存消耗下通过本题。
>
> ## 题目描述
>
> 如题，给定一个范围 n，有 q 个询问，每次输出第 k 小的素数。
>
> ## 输入格式
>
> 第一行包含两个正整数 n,q，分别表示查询的范围和查询的个数。
>
> 接下来 q 行每行一个正整数 k，表示查询第 k 小的素数。
>
> ## 输出格式
>
> 输出 q 行，每行一个正整数表示答案。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 100 5
> 1
> 2
> 3
> 4
> 5
> ```
>
> **输出 #1**
>
> ```
> 2
> 3
> 5
> 7
> 11
> ```
>
> ## 说明/提示
>
> 【数据范围】
>  对于 100% 的数据，n=108，1≤q≤106，保证查询的素数不大于 n。
>
> Data by NaCly_Fish.

本题我们之间构建一个数组专门装primes然后一次问询输出即可。

代码如下：

```c++
#include <iostream>
using namespace std;

bool vis[100000008];
int primes[100000008], cnt;

void getprime(int n) {
    for (int i = 2; i <= n; ++i) {
        if (!vis[i])
            primes[++cnt] = i;
        for (int j = 1; j <= cnt && i * primes[j] <= n; j++) {
            vis[i * primes[j]] = 1;
            if (i % primes[j] == 0)
                break;
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    long long x, y;
    cin >> x >> y;
    getprime(x);

    for (int i = 0; i < y; ++i) {
        int temp;
        cin >> temp;
        cout << primes[temp] << '\n';
    }

    return 0;
}
```

#### 题目3：

> ## 题目背景
>
> 题目名称是吸引你点进来的。
> 实际上该题还是很水的。
>
> ## 题目描述
>
> 给定 l,r，求区间 [l,r] 内质数的个数。
>
> ## 输入格式
>
> 第一行有两个整数，分别代表询问次数 n 和给定区间的右端点最大值 m。
>
> 接下来 n 行，每行两个整数 l,r，代表一次查询。
>
> ## 输出格式
>
> 对于每次查询输出一行，若 l,r∈[1,m]，则输出区间质数个数，否则输出 `Crossing the line`。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 2 5
> 1 3
> 2 6
> ```
>
> **输出 #1**
>
> ```
> 2
> Crossing the line
> ```
>
> ## 说明/提示
>
> #### 数据范围与约定
>
> - 对于 20% 的数据，保证 n,m≤10。
> - 对于 100% 的数据，保证 1≤n≤1000，1≤m≤10^6，−10^9≤l≤r≤10^9。

对于这道题，我们直接通过欧拉筛来获得区间的所有质数，再通过``upper_bound``和``lower_bound``获得相应的地址的距离差，即为区间内质数的数量。最后注意判断``l``与``r``满足不满足在范围的条件，对其进行判断即可。

代码如下：

```c++
#include <algorithm>
#include <iostream>
using namespace std;

bool vis[100000008];
int primes[100000008], cnt;

void getprime(int n) {
    for (int i = 2; i <= n; ++i) {
        if (!vis[i])
            primes[++cnt] = i;
        for (int j = 1; j <= cnt && i * primes[j] <= n; j++) {
            vis[i * primes[j]] = 1;
            if (i % primes[j] == 0)
                break;
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n, m;
    cin >> n >> m;
    getprime(m);

    for (int i = 0; i < n; ++i) {
        int templ, tempr;
        cin >> templ >> tempr;
        if (tempr < 1 || templ < 1 || tempr > m || templ > m) {
            cout << "Crossing the line" << '\n';
            continue;
        }
        auto r = upper_bound(primes + 1, primes + cnt + 1, tempr);
        auto l = lower_bound(primes + 1, primes + cnt + 1, templ);
        cout << r - l << '\n';
    }

    return 0;
}

```

---

## 三、分解质因数

### 3.1 算数基本定理

任何一个大于1的自然数，都可以唯一分解成有限个质数的乘积。
$$
N = p_1^{\alpha1} · p_2^{\alpha2} · p_3^{\alpha3} ······
$$
上式的\(p_1 < p_2 < p_3 < ......\) 都是质数，\(\alpha_i\)为正整数。

就比如\(18 =  2 × 3 × 3\)。

### 3.2 试除法

从\(2\)遍历到\(\sqrt n\)，整除\(n\)看能否整除，循环直到不能整除，记录质因数。

模板如下：

```c++
vector<int> factors(int n) {
	vector<int> ans;
    for (int i = 2; i * i <= n; ++i) {
        while (n % i == 0) {
            n = n / i;
        }
        ans.push_back(i);
    }
    if (n > 1) ans.push_back(n);
    return ans;
}
```

时间复杂度：\(O(\sqrt n)\)，适用于单次查询或者\(n\)相对较小的情况。

### 3.3 线性筛优化法

当需要多次查询分解质因数时，试除法效率不够。可以利用线性筛预处理每个数的最小质因数，然后以 \(O(log\ n)\)的时间复杂度完成单次分解。

在实现时，我们将其分为两个步骤：

- 预处理
- 快速分解

预处理部分时，我们在进行线性筛的同时，记录每个数被哪个质数筛掉，即它的最小质因数。

代码如下：

```c++
int min_prime[N];

void init(int n) {
    for (int i = 2; i <= n; ++i) {
        if (!min_prime[i]) {
            min_prime[i] = i;
            primes[++cnt] = i;
        }
        for (int j = 1; j <= cnt && primes[j] * i <= n; ++j) {
            min_prime[i * primes[j]] = primes[j];
            if (i % primes[j] == 0) break;
        }
    }
}
```

在有了最小质因数表后，我们不断除以当前的最小质因数即可。

```c++
int factor(int n) {
    while (n > 1) {
        int p = min_prime[n];
        while (n % p == 0) {
            n = n / p;
        }
        return n;
    }
    return n;
}
```

就这样。

#### 题目4：

> ## 题目描述
>
> 已知正整数 n 是两个不同的质数的乘积，试求出两者中较大的那个质数。
>
> ## 输入格式
>
> 输入一个正整数 n。
>
> ## 输出格式
>
> 输出一个正整数 p，即较大的那个质数。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 21
> ```
>
> **输出 #1**
>
> ```
> 7
> ```
>
> ## 说明/提示
>
> 1≤n≤2×109。
>
> NOIP 2012 普及组 第一题

本题直接遍历2到n，输出第一个得到的最大素数。

```c++
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    for (int i = 2; i < n; i++) {
        if (n % i == 0) {
            cout << n / i;
            return 0;
        }
    }
    return 0;
}
```

---

## 四、快速幂

### 4.1 普通快速幂

在计算一个较为大的指数的时候，比如\(a^n\)时，直接暴力计算时间复杂度是\(O(n)\)，而快速幂可以实现\(O(log\ n)\)。

一个思路是将其通过指数运算变形。

我们知道，\(a^x × a^y = a^{x + y}\)。如果我们将指数进行拆分，拆分成\(2^n\)的倍数，刚刚好和该指数的二进制位数对应。

与此同时，我们还知道\(a^x × a^x = a^{2x}\)，当我们处理到新的一位时，如果该位上为1，则把\(a^x × a^x = a^{2x}\)得到的数乘以当前的底数即可。

与此同时，大部分快速幂还需要我们对结果求模，我们知道求模运算在加减乘的运算中可以直接带入相乘。

> [!WARNING]
>
> 只有加减乘可以，除法不行。
>
> 除法的相关问题会在下文进行说明与转化。

则我们得到了快速幂的模板：

```c++
long long qpow(long long a, long long b, long long MOD) {
	long long ans = 1;
    while (b) {
        if (b & 1)
            ans = ans * a % MOD;
        a = a * a % MOD;
        b >>= 1;
    }
    return ans;
}
```

### 4.2 矩阵快速幂

矩阵快速幂 = 矩阵乘法 + 快速幂

⽤于快速计算矩阵的 k 次幂，常⽤于求解递推数列（如斐波那契）的第 n 项。

具体相关的方法与应用会在线性代数这门课程说明 ~~最起码我现在还不知道~~

矩阵乘法

设\(C = A × B\)，则：
$$
c_{ij}= \sum_{k = 1}^n a_{ik} × b_{kj}
$$
即结果矩阵的第 i ⾏第 j 列，等于 A 的第 i 行与 B 的第 j 列对应元素乘积之和。~~wow这玩意看着好高级~~

单位矩阵：

主对角线全为 1，其余为 0 的矩阵，记为 \(I\) 。性质： \(A × I = A\)

综上（~~大概吧~~）我们能得到矩阵快速幂的模板：

```c++
const int MOD = 1e9 + 7;
const int N = 110;

struct Matrix {
    long long m[N][N];
    Matrix() {
        memset(m, 0, sizeof(m));
    }
};

Matrix operator*(const Matrix & a, const Matrix & b) {
    Matrix c;
    for (int i = 1; i <= n; ++i)
        for (int k = 1; k <= n; ++k)
            for (int j = 1; j <= n; ++j)
                c.m[i][j] = (c.m[i][j] + a.m[i][k] * b.m[k][j]) % MOD;
    return c;
}

Matrix matrix_pow(Matrix a, long long k) {
    Matrix ans;
    for (int i = 1; i <= n; ++i)
        ans.m[i][i] = 1;
    while (k) {
        if (k & 1)
            ans = ans * a;
        a = a * a;
        k >>= 1;
    }
    return ans;
}
```

大抵就是这样。

#### 题目5：

> ## 题目描述
>
> 给你三个整数 a,b,p，求 abmodp。
>
> ## 输入格式
>
> 输入只有一行三个整数，分别代表 a,b,p。
>
> ## 输出格式
>
> 输出一行一个字符串 `a^b mod p=s`，其中 a,b,p 分别为题目给定的值， s 为运算结果。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 2 10 9
> ```
>
> **输出 #1**
>
> ```
> 2^10 mod 9=7
> ```
>
> ## 说明/提示
>
> **样例解释**
>
> 210=1024，1024mod9=7。
>
> **数据规模与约定**
>
> 对于 100% 的数据，保证 0≤a,b<2^31，a+b>0，2≤p<2^31。

本题直接套用模板即可，注意输出的形式就好啦。

```c++
#include <iostream>
using namespace std;

long long qpow(long long a, long long b, long long MOD) {
    long long ans = 1;
    while (b) {
        if (b & 1)
            ans = ans * a % MOD;
        a = a * a % MOD;
        b >>= 1;
    }
    return ans;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    long long a, b, p;
    cin >> a >> b >> p;

    cout << a << "^" << b << " mod " << p << "=" << qpow(a, b, p);

    return 0;
}
```

#### 题目6：

> ## 题目背景
>
> 一个 m×n 的**矩阵**是一个由 m 行 n 列元素排列成的矩形阵列。
>
> 本题中认为矩阵中的元素 aij 是整数。
>
> 两个大小分别为 m×n 和 n×p 的矩阵 A,B **相乘**的结果为一个大小为 m×p 的矩阵。将结果矩阵记作 C，则
>
> cij=k=1∑naikbkj,(1≤i≤m, 1≤j≤p).
>
> 而如果 A 的列数与 B 的行数不相等，则无法进行乘法。
>
> 可以验证，矩阵乘法满足结合律，即 (AB)C=A(BC)。
>
> 一个大小为 n×n 的矩阵 A 可以与自身进行乘法，得到的仍是大小为 n×n 的矩阵，记作 A2=A×A。进一步地，还可以递归地定义任意高次方 Ak=A×Ak−1，或称 Ak=k 次A×A×⋯×A。
>
> 特殊地，定义 A0 为单位矩阵 I=10⋮001⋮0⋯⋯⋱⋯00⋮1。
>
> ## 题目描述
>
> 给定 n×n 的矩阵 A，求 Ak。
>
> ## 输入格式
>
> 第一行两个整数 n,k。
> 接下来 n 行，每行 n 个整数，第 i 行的第 j 的数表示 Ai,j​。
>
> ## 输出格式
>
> 输出 Ak
>
> 共 n 行，每行 n 个数，第 i 行第 j 个数表示 (Ak)i,j，每个元素对 109+7 取模。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 2 1
> 1 1
> 1 1
> ```
>
> **输出 #1**
>
> ```
> 1 1
> 1 1
> ```
>
> **输入 #2**
>
> ```
> 3 5
> 1 2 3
> 4 5 6
> 7 8 9
> ```
>
> **输出 #2**
>
> ```
> 121824 149688 177552
> 275886 338985 402084
> 429948 528282 626616
> ```
>
> ## 说明/提示
>
> **【数据范围】**
>
> 对于 100% 的数据，1≤n≤100，0≤k≤1012，∣Ai,j∣≤1000。

同理，套入上面的模板即可。

代码如下：

```c++
#include <cstring>
#include <iostream>
using namespace std;

const int MOD = 1e9 + 7;
const int N = 110;
int n;

struct Matrix {
    long long m[N][N];
    Matrix() {
        memset(m, 0, sizeof(m));
    }
};

Matrix operator*(const Matrix & a, const Matrix & b) {
    Matrix c;
    for (int i = 1; i <= n; ++i)
        for (int k = 1; k <= n; ++k)
            for (int j = 1; j <= n; ++j)
                c.m[i][j] = (c.m[i][j] + a.m[i][k] * b.m[k][j]) % MOD;
    return c;
}

Matrix matrix_pow(Matrix a, long long k) {
    Matrix ans;
    for (int i = 1; i <= n; ++i)
        ans.m[i][i] = 1;
    while (k) {
        if (k & 1)
            ans = ans * a;
        a = a * a;
        k >>= 1;
    }
    return ans;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    long long a, b;
    cin >> a >> b;
    Matrix mat;

    n = a;

    for (int i = 1; i <= a; ++i) {
        for (int j = 1; j <= a; ++j)
            cin >> mat.m[i][j];
    }

    Matrix ans = matrix_pow(mat, b);

    for (int i = 1; i <= a; ++i) {
        for (int j = 1; j <= a; ++j)
            cout << ans.m[i][j] << ' ';
        cout << '\n';
    }


    return 0;
}
```

---

## 五、裴蜀定理与拓展欧几里得算法

### 5.1 裴蜀定理

裴蜀定理，Bézout's lemma，给出了一个整数能够表示为两个整数的整系数线性组合的充分必要条件。

对于两个不全为 0 的整数 \(a\) 和 \(b\) ，⼀定存在整数 \(x, y\) 使得 \(ax + by = gcd(a, b)\)。

 \(a\) 和 \(b\) 的线性组合（整数倍相加）⼀定可以凑出它们的最⼤公约数。

如果我们将其进行推广，则有：

- 在 \(gcd(a,b) =1\)时的特殊情形，可以得到整数 \(a\) 和 \(b\) 互素，当且仅当存在整数 \(x\) 和 \(y\) ，使得\(ax + by = 1\)成立。

- 可以凑出 \(gcd(a, b)\) 的任意整数倍： \(ax + by = k ⋅ gcd(a, b)\)

- ⽅程 \(ax + by = c\) 有整数解的充要条件： \(gcd(a, b) ∣ c\)

- 可推⼴到 \(n\) 个整数：
  $$
  a_1x_1+a_2x_2+a_3x_3+a_4x_4+...+a_nx_n=gcd(a_1,a_2,a_3,a_4,...,a_n)
  $$

> [!NOTE]
>
> 此部分内容证明如下：
>
> 记 \(d = \gcd(a, b)\)。因为 \(d \mid a, b\)，所以存在整数 \(u, v\) 使得 \(a = du\), \(b = dv\) 成立。因此，总有
> $$
> ax + by = d(ux + vy).
> $$
>
> 这就说明 \(d \mid ax + by\)。
>
> 反过来，需要说明存在 \(x, y\) 使得等式成立。如果 \(a, b\) 之一是 \(0\)，不妨设 \(b = 0\)，那么它们的最大公约数为 \(d = a\)，显然有 \((x, y) = (1, 0)\) 使得等式成立。接下来，考虑 \(a, b\) 均不为零的情形。由于 \(\gcd(a, b) = \gcd(-a, b) = \gcd(a, -b)\)，所以不妨设 \(a, b\) 都是正数。
>
> 考虑辗转相除法的过程，有
>
> $$
> \begin{aligned}
> a   &= q_1b   + r_1, && 0\le r_1 < b,\\
> b   &= q_2r_1 + r_2, && 0\le r_2 < r_1,\\
> r_1 &= q_3r_2 + r_3, && 0\le r_3 < r_2,\\
>  & \cdots \\
> r_{n-3} &= q_{n-1}r_{n-2} + r_{n-1}, && 0\le r_{n-1} < r_{n-2},\\
> r_{n-2} &= q_nr_{n-1} + r_n,         && 0\le r_n     < r_{n-1},\\
> r_{n-1} &= q_{n+1}r_n.
> \end{aligned}
> $$
>
> 由于最大公约数是 \(d\)，最后一步辗转相除时，一定有 \(r_n = d\)。所以，倒数第二个等式可以写作
>
> $$
> d = r_n = r_{n-2} - q_n r_{n-1}.
> $$
>
> 从倒数第三个等式中解出
>
> $$
> r_{n-1} = r_{n-3} - q_{n-1} r_{n-2}
> $$
>
> 再代入上式，就可以消去 \(r_{n-1}\)：
>
> $$
> \begin{aligned}
> d &= r_{n-2} - q_n(r_{n-3} - q_{n-1} r_{n-2}) \\
> &= (1 + q_n q_{n-1}) r_{n-2} - q_n r_{n-3}.
> \end{aligned}
> $$
>
> 类似地，可以逐步地消去所有 \(r_{n-2}, r_{n-3}, \dots, r_2, r_1\)，最终得到
>
> $$
> d = xa + yb.
> $$
>
> 这就证明了存在 \(x, y\) 使得 \(ax + by = d\) 成立。由前文分析可知，这也证明了原命题。

此部分证明来自[OI-Wiki](https://oi-wiki.org/math/number-theory/bezouts/)。

此处，关于存在性的证明是构造性的，它同时给出了该系数的一种计算方法。这一计算方法就是扩展欧几里得算法。

### 5.2 扩展欧几里得算法

扩展欧几里得算法，Extended Euclidean algorithm（EXGCD），扩展欧⼏⾥得算法可以在求\(gcd(a,b)\)的同时，求出⽅程\(ax+by=gcd(a,b)\)的⼀组整数解\(x,y\)。

> [!NOTE]
>
> AI辅助提供的证明如下：
>
> **数学基础：**
>
> 扩展欧几里得算法的根基是带余除法和最大公约数的性质。
>
> - **带余除法**：对于任意整数 \(a, b\)（\(b \neq 0\)），存在唯一的整数 \(q, r\)，使得 \(a = bq + r\)，其中 \(0 \le r < |b|\)。
> - \(gcd\)递归性质：\(\gcd(a, b) = \gcd(b, r)\)，其中 \(r = a \bmod b\)。 
>   *证明思路*：若 \(d \mid a\) 且 \(d \mid b\)，则 \(d \mid (a - bq) = r\)；反之，若 \(d \mid b\) 且 \(d \mid r\)，则 \(d \mid (bq + r) = a\)。所以两者的公约数集合完全相同，最大公约数自然相等。
>
> ---
>
> **算法的递归构造：**
>
> 我们使用数学归纳法来证明：对任意整数对 \((a, b)\)（设 \(b > 0\)），必定存在整数 \(x, y\) 使 \(ax + by = \gcd(a, b)\)。
>
> - **基础情况**：当 \(b = 0\) 时，\(\gcd(a, 0) = a\)。此时取 \(x = 1, y = 0\)，等式 \(a \cdot 1 + 0 \cdot 0 = a\) 成立。
>
> - **归纳步骤**：假设对于所有绝对值小于 \(b\) 的整数对，命题成立。 
>   令 \(r = a \bmod b\)，则 \(a = qb + r\)（\(0 \le r < b\)）。 
>   根据归纳假设，对于数对 \((b, r)\)，存在整数 \(x', y'\) 满足：
>   $$
>   b x' + r y' = \gcd(b, r)
>   $$
>   由第1部分的引理，\(\gcd(b, r) = \gcd(a, b)\)。将 \(r = a - qb\) 代入上式：
>   $$
>   b x' + (a - qb) y' = a \cdot y' + b \cdot (x' - qy') = \gcd(a, b)
>   $$
>   因此，我们成功构造出了原方程的一组特解：
>   $$
>   x = y', \quad y = x' - qy'
>   $$
>   由于欧几里得算法的余数序列严格递减（\(b > r > ...\)），该递归过程必然在有限步内终止于 \(b=0\)。存在性得证。
>
> ---
>
> **算法正确性的完整验证：**
>
> 设递归调用返回的解为 \((x', y')\)，满足 \(b x' + r y' = d\)（其中 \(d = \gcd(a, b)\)）。
> 计算 \(a x + b y\)：
> $$
> a(y') + b(x' - qy') = a y' + b x' - bq y' = b x' + (a - bq) y' = b x' + r y' = d
> $$
>
> ---
>
> **全部整数解的通解形式：**
>
> 设已找到一组特解 \((x_0, y_0)\) 满足 \(a x_0 + b y_0 = d\)（其中 \(d = \gcd(a, b)\)）。
>
> 令 \((x, y)\) 为任意一组解，即 \(a x + b y = d\)。两式相减：
> $$
> a(x - x_0) + b(y - y_0) = 0
> $$
> 两边同除以 \(d\)，令 \(a' = a/d\)，\(b' = b/d\)，则 \(\gcd(a', b') = 1\)：
> $$
> a'(x - x_0) = -b'(y - y_0)
> $$
> 因为 \(b' \mid a'(x - x_0)\) 且 \(\gcd(a', b') = 1\)，根据欧几里得引理，必有 \(b' \mid (x - x_0)\)。  
> 设 \(x - x_0 = b' t\)（\(t\) 为任意整数），代入上式得 \(y - y_0 = -a' t\)。
>
> 因此，完整的通解为：
> $$
> \boxed{x = x_0 + \frac{b}{d} t, \quad y = y_0 - \frac{a}{d} t \quad (t \in \mathbb{Z})}
> $$

递归边界：

当 \(b = 0\)时， \(gcd(a, 0) = a\) ，此时 \(x = 1, y = 0\) 是⼀组解。

模板如下：

```c++
long long exgcd(long long a, long long b, long long &x, long long &y) {
    if (b == 0) {
        x = 1;
        y = 0;
        return a;
    }
    long long d = exgcd(b, a % b, y, x);
    y = y - (a / b) * x;
    return d;
}
```

#### 题目7：

> ## 题目描述
>
> 给定一个包含 n 个元素的**整数**序列 A，记作 A1,A2,A3,...,An。
>
> 求另一个包含 n 个元素的待定**整数**序列 X，记 S=i=1∑nAi×Xi，使得 S>0 且 S 尽可能的小。
>
> ## 输入格式
>
> 第一行一个整数 n，表示序列元素个数。
>
> 第二行 n 个整数，表示序列 A。
>
> ## 输出格式
>
> 一行一个整数，表示 S>0 的前提下 S 的最小值。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 2
> 4059 -1782
> ```
>
> **输出 #1**
>
> ```
> 99
> ```
>
> ## 说明/提示
>
> 对于 100% 的数据，1≤n≤20，∣Ai∣≤10^5，且 A 序列不全为 0。

答案就是所有非零 \(A_i\) 的绝对值的最大公约数。

原因如下：

设 \(g=gcd⁡(∣A1∣,∣A2∣,…,∣An∣)g=gcd(∣A1∣,∣A2∣,…,∣An∣)\)

根据裴蜀定理，存在一组整数 \(X_i\)，使得 \(\sum_{i=1}^nA_iX_i = g\)

而任意整数线性组合 \(S = \sum_{i=1}^nA_iX_i\)

一定都是 \(g\) 的倍数。所以满足 \(S>0\) 的最小值就是 \(g\)。

综上，代码如下：

```c++
#include <cstring>
#include <iostream>
using namespace std;

int n;

int gcd(int a, int b) {
    return b == 0 ? a : gcd (b, a % b);
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> n;
    int ans = 0, temp;
    cin >> ans;
    ans = abs(ans);
    for (int i = 1; i < n; ++i) {
        cin >> temp;
        ans = gcd(ans, abs(temp));
    }

    cout << ans;


    return 0;
}
```

#### 题目8：

> ## 题目描述
>
> 求关于 x 的同余方程 ax≡1(modb) 的最小正整数解。
>
> ## 输入格式
>
> 一行，包含两个整数 a,b，用一个空格隔开。
>
> ## 输出格式
>
> 一个整数 x0，即最小正整数解。输入数据保证一定有解。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 3 10
> ```
>
> **输出 #1**
>
> ```
> 7
> ```
>
> ## 说明/提示
>
> ### 数据规模与约定
>
> - 对于 40% 的数据，2≤b≤103；
> - 对于 60% 的数据，2≤b≤5×107；
> - 对于 100% 的数据，2≤a,b≤2×109。

套入模板，并注意最后输出结果时要处理\(x\)为最小的正数解。

代码如下：

```c++
#include <cstring>
#include <iostream>
using namespace std;

int n;

int gcd(int a, int b) {
    return b == 0 ? a : gcd (b, a % b);
}

long long exgcd(long long a, long long b, long long &x, long long &y) {
    if (b == 0) {
        x = 1;
        y = 0;
        return a;
    }
    long long d = exgcd(b, a % b, y, x);
    y = y - (a / b) * x;
    return d;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    long long a, b;
    cin >> a >> b;
    long long x = 0, y = 0;
    exgcd(a, b, x, y);
    x = (x % b + b) % b;
    cout << x;

    return 0;
}
```

---

## 
