# CUGBACM25级暑假小学期训练 #3 动态规划基础

## 一、背包问题

### 1.1 01背包

> **给出多个物品，体积为 ，价值为 ，问在背包容量**为**V**条件下价值最大为多少

在上述例题中，由于每个物体只有两种可能的状态（取与不取），对应二进制中的 0 和 1，这类问题便被称为「0-1 背包问题」。

> [!IMPORTANT]
>
> 01背包问题中的物品不能重复选取

一种用``0``和``1``来表示背包内容选取与否的方法。

我们可以从以下几个步骤入手：

- 分析状态表示方法
- 确定边界情况与遍历顺序
- 分析状态转移方程
- 推导证明

首先是分析状态表示情况，我们在数据量小的时候，我们只需要一个二维数组``dp[i, j]``，就能记录当前情况下的最优价值与剩余空间。

然后两个边界是分别无物品和无空间，即``i`` ``j``分别都为 0 时候的情况。并且由于我们获得这个边界的方法是通过递归由小到大，所以遍历方向也是由小到大。在下文提出转移方程后，我们发现每个转移方程中与``i - 1``个物品有关系，所以我们从``i = 0`` ``j ++``开始遍历。

然后状态转移方程部分，假设前面``i - 1``个物品的状态已经处理完成，对于第``i``个物品来说，他可以选择放入背包，也可以选择不放背包。不放入背包有两种原因，一种是剩余空间无法放入，另外一种是放入背包后其产生的相同占用所产生的价值小于上一层获得的最优状态，那我直接用上一层的来不就可以了。不放入的情况下，直接继承``dp[i - 1][j]``项。放入的情况下，就需要我们修改当前为``dp[i - 1][j - weight[i]] + value[i]``。其中``dp[i - 1][j - weight[i]]``指选择了物品``i``后所剩空间全部填充为已经计算过的产生价值最大的最优状态。

所以这部分代码可以如下表示：

```c++
#define N 1005
int dp[N][N];
int weight[N], value[N]; //分别为质量大小和价值大小

for (int i = 1; i <= n; ++i) {
    for (int j = 1; j <= m; ++j) { //此处的n为物品数量 m为空间大小
  		if (j < weight[i]) {
        	//放不下 直接继承
        	dp[i][j] = dp[i - 1][j];
    	}
        else {
            dp[i][j] = max(dp[i - 1][j], dp[i - 1][j - weight[i]] + value[i]);
        }
    }
}
```

大体就是这个样子。

我们来分析一下他和穷举的复杂度。

|            | 穷举 | 二维DP |
| :--------: | :--: | :----: |
| 时间复杂度 | 2^n  | n * m  |
| 空间复杂度 |  n   | n * m  |

有没有方法优化呢，我们来试着把其第一维扔掉，直接继承。

代码如下修改表示：

```c++
#define N 1005
int dp[[N];
int weight[N], value[N]; //分别为质量大小和价值大小

for (int i = 1; i <= n; ++i) {
    for (int j = 1; j <= m - weight[i]; ++j) { //此处的n为物品数量 m为空间大小
            dp[j] = max(dp[j], dp[j - weight[i]] + value[i]);
        }
    }
}
```

大体就是应该是这个样子...吗？

其实是不对的，这个时候j如果从左往右，会出现同一个物品取两次或者更多的情况，01背包同一个物品只允许选一次，矛盾。所以第二层循环是从右侧向左侧。

继续把代码进行修改，如下：

```c++
#define N 1005
int dp[[N];
int weight[N], value[N]; //分别为质量大小和价值大小

for (int i = 1; i <= n; ++i) {
    for (int j = m; j <= weight[i]; --j) { //此处的n为物品数量 m为空间大小
            dp[j] = max(dp[j], dp[j - weight[i]] + value[i]);
        }
    }
}
```

大体就是应该是这个样子吧

#### 题目1：

> ## 题目描述
>
> 棋盘上 A 点有一个过河卒，需要走到目标 B 点。卒行走的规则：可以向下、或者向右。同时在棋盘上 C 点有一个对方的马，该马所在的点和所有跳跃一步可达的点称为对方马的控制点。因此称之为“马拦过河卒”。
>
> 棋盘用坐标表示，A 点 (0,0)、B 点 (n,m)，同样马的位置坐标是需要给出的。
>
> ![img](https://cdn.luogu.com.cn/upload/image_hosting/ipmwl52i.png)
>
> 现在要求你计算出卒从 A 点能够到达 B 点的路径的条数，假设马的位置是固定不动的，并不是卒走一步马走一步。
>
> ## 输入格式
>
> 一行四个正整数，分别表示 B 点坐标和马的坐标。
>
> ## 输出格式
>
> 一个整数，表示所有的路径条数。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 6 6 3 3
> ```
>
> **输出 #1**
>
> ```
> 6
> ```
>
> ## 说明/提示
>
> 对于 100% 的数据，1≤n,m≤20，0≤ 马的坐标 ≤20。
>
> 保证起点不是马的控制点。
>
> **【题目来源】**
>
> NOIP 2002 普及组第四题

本题我们先考虑没有马的影响，则我们在每个点的情况如果用``dp``表示，可以表示为``dp[i][j]``这个点到原点的路径条数。但是由于``(0,0)``这个数不好循环，我们可以设原点为``(1,1)``。则有以下状态转移方程：
$$
dp(i,j) = dp(i - 1, j) + dp(i, j - 1)
$$
然后考虑有马的情况，我们可以设如果该点有马，则直接设其``dp``为 0。

最后就是注意``dp``可能比较大，就这样。

代码如下：

```c++
#include <iostream>

using namespace std;

int horse[25][25], hxp[] = {0, -2, -1, 1, 2, 2, 1, -1, -2}, hyp[] = {0, 1, 2, 2, 1, -1, -2, -2, -1};
long long dp[25][25];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int bx, by, hx, hy;
    cin >> bx >> by >> hx >> hy;

    bx ++, by ++, hx ++, hy ++;

    dp[1][0] = 1;
    horse[hx][hy] = 1;

    for (int i = 1; i <= 8; ++i) {
        if (hx + hxp[i] < 1 || hy + hyp[i] < 1) continue;
        horse[hx + hxp[i]][hy + hyp[i]] = 1;
    }

    for (int i = 1; i <= bx; ++i) {
        for (int j = 1; j <= by; ++j) {
            if (horse[i][j]) {
                dp[i][j] = 0;
                continue;
            }
            dp[i][j] = dp[i - 1][j] + dp[i][j - 1];
        }
    }
    cout << dp[bx][by];

    return 0;
}
```

#### 题目2：

> ## 题目描述
>
> 观察下面的数字金字塔。
>
> 写一个程序来查找从最高点到底部任意处结束的路径，使路径经过数字的和最大。每一步可以走到左下方的点也可以到达右下方的点。
>
> ![img](https://cdn.luogu.com.cn/upload/image_hosting/95pzs0ne.png)
>
> 在上面的样例中，从 7→3→8→7→5 的路径产生了最大权值。
>
> ## 输入格式
>
> 第一个行一个正整数 r，表示行的数目。
>
> 后面每行为这个数字金字塔特定行包含的整数。
>
> ## 输出格式
>
> 单独的一行，包含那个可能得到的最大的和。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 5
> 7
> 3 8
> 8 1 0
> 2 7 4 4
> 4 5 2 6 5 
> ```
>
> **输出 #1**
>
> ```
> 30
> ```
>
> ## 说明/提示
>
> 【数据范围】
>  对于 100% 的数据，1≤r≤1000，所有输入在 [0,100] 范围内。
>
> 题目翻译来自 NOCOW。
>
> IOI1994 Day1T1 / USACO Training Section 1.5。

这题简单，我们直接建一个二维数组，然后遍历。

状态转移方程如下：
$$
dp(i, j) = max(dp(i - 1, j - 1), dp(i - 1, j)) + matrix[i][j]
$$
唯一要注意的是这个边界，小心越界，还有输出的时候要注意不是取``dp[n][n]``，要找第``n``排最大的，就酱。

代码如下：

```c++
#include <iostream>

using namespace std;
int matrix[1005][1005], dp[1005][1005];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n;
    cin >> n;

    for (int i = 1; i <= n; ++i) {
        for (int j = 1; j <= i; ++j) {
            cin >> matrix[i][j];
        }
    }

    for (int i = 1; i <= n; ++i) {
        for (int j = 1; j <= n; ++j) {
            dp[i][j] = max(dp[i - 1][j - 1], dp[i - 1][j]) + matrix[i][j];
        }
    }
    int max = 0;
    for (int i = 1; i <= n; ++i) {
        if (max < dp[n][i]) max = dp[n][i];
    }
    cout << max;
    return 0;
}
```

#### 题目3：

> ## 题目描述
>
> 辰辰是个天资聪颖的孩子，他的梦想是成为世界上最伟大的医师。为此，他想拜附近最有威望的医师为师。医师为了判断他的资质，给他出了一个难题。医师把他带到一个到处都是草药的山洞里对他说：“孩子，这个山洞里有一些不同的草药，采每一株都需要一些时间，每一株也有它自身的价值。我会给你一段时间，在这段时间里，你可以采到一些草药。如果你是一个聪明的孩子，你应该可以让采到的草药的总价值最大。”
>
> 如果你是辰辰，你能完成这个任务吗？
>
> ## 输入格式
>
> 第一行有 2 个整数 T（1≤T≤1000）和 M（1≤M≤100），用一个空格隔开，T 代表总共能够用来采药的时间，M 代表山洞里的草药的数目。
>
> 接下来的 M 行每行包括两个在 1 到 100 之间（包括 1 和 100）的整数，分别表示采摘某株草药的时间和这株草药的价值。
>
> ## 输出格式
>
> 输出在规定的时间内可以采到的草药的最大总价值。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 70 3
> 71 100
> 69 1
> 1 2
> ```
>
> **输出 #1**
>
> ```
> 3
> ```
>
> ## 说明/提示
>
> **【数据范围】**
>
> - 对于 30% 的数据，M≤10；
> - 对于全部的数据，M≤100。
>
> **【题目来源】**
>
> NOIP 2005 普及组第三题

这感觉是最正宗的01背包问题了吧（

状态转移方程如下（如果满足转移条件）：
$$
dp(i, j) = max(dp(i - 1, j), dp(i - 1)(j - weight(i)) + value(i))
$$
注意数组大小，被坑了 >_<

代码如下：

```c++
#include <iostream>

using namespace std;
int weight[1005], dp[1005][1005], value[1005];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int t, n;
    cin >> t >> n;

    for (int i = 1; i <= n; ++i) {
        cin >> weight[i] >> value[i];
    }

    for (int i = 1; i <= n; ++i) {
        for (int j = 1; j <= t; ++j) {
            if (j < weight[i]) {
                dp[i][j] = dp[i - 1][j];
                continue;
            }
            dp[i][j] = max(dp[i -1][j], dp[i - 1][j - weight[i]] + value[i]);
        }
    }

    cout << dp[n][t];

    return 0;
}
```

#### 题目4：

> ## 题目描述
>
> 有一个箱子容量为 V，同时有 n 个物品，每个物品有一个体积。
>
> 现在从 n 个物品中，任取若干个装入箱内（也可以不取），使箱子的剩余空间最小。输出这个最小值。
>
> ## 输入格式
>
> 第一行共一个整数 V，表示箱子容量。
>
> 第二行共一个整数 n，表示物品总数。
>
> 接下来 n 行，每行有一个正整数，表示第 i 个物品的体积。
>
> ## 输出格式
>
> - 共一行一个整数，表示箱子最小剩余空间。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 24
> 6
> 8
> 3
> 12
> 7
> 9
> 7
> ```
>
> **输出 #1**
>
> ```
> 0
> ```
>
> ## 说明/提示
>
> 对于 100% 数据，满足 0<n≤30，1≤V≤20000。
>
> **【题目来源】**
>
> NOIP 2001 普及组第四题

这题我们可以直接把他的价值等于空间，最后输出的时候相减即可。

代码如下：

```c++
#include <iostream>

using namespace std;
int weight[1005], dp[20005], value[1005];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int v, n;
    cin >> v;
    cin >> n;

    for (int i = 1; i <= n; ++i) {
        cin >> weight[i];
        value[i] = weight[i];
    }

    for (int i = 1; i <= n; ++i) {
        for (int j = v; j >= weight[i]; --j) {
            dp[j] = max(dp[j], dp[j - weight[i]] + value[i]);
        }
    }

    cout << v - dp[v];

    return 0;
}
```

---

### 1.2 完全背包

> 给出多个物品，体积为 ，价值为 ，每个物品无限次使用，问在背包容量为 的条件下价值最大为多少

与``01背包``不同，完全背包不管你每种物品使用多少次 ~~你想咋使就咋使~~ 

他的状态转移方程怎么弄呢？

其实就是先继承上一层的状态，然后计算当前要不要放入物品i。

代码如下：

```c++
#define N 1005
int dp[N][N];
int weight[N], value[N]; //分别为质量大小和价值大小

for (int i = 1; i <= n; ++i) {
    for (int j = 1; j <= m; ++j) { //此处的n为物品数量 m为空间大小
        //先继承
        dp[i][j] = dp[i - 1][j];
        if(j >= w[i]) {
            dp[i][j] = max(dp[i][j], dp[i][j - weight[i]] + value[i]);
        }
    }
}
```

然后这个优化到一维甚至都不用再重复继承了，wow。

代码如下：

```c++
#define N 1005
int dp[[N];
int weight[N], value[N]; //分别为质量大小和价值大小

for (int i = 1; i <= n; ++i) {
    for (int j = w[i]; j <= m; ++j) { //此处的n为物品数量 m为空间大小
            dp[j] = max(dp[j], dp[j - weight[i]] + value[i]);
        }
    }
}
```

大概就是这个样子。

#### 题目5：

> ## 题目背景
>
> 此题为纪念 LiYuxiang 而生。
>
> ## 题目描述
>
> LiYuxiang  是个天资聪颖的孩子，他的梦想是成为世界上最伟大的医师。为此，他想拜附近最有威望的医师为师。医师为了判断他的资质，给他出了一个难题。医师把他带到一个到处都是草药的山洞里对他说：“孩子，这个山洞里有一些不同种类的草药，采每一种都需要一些时间，每一种也有它自身的价值。我会给你一段时间，在这段时间里，你可以采到一些草药。如果你是一个聪明的孩子，你应该可以让采到的草药的总价值最大。”
>
> 如果你是 LiYuxiang，你能完成这个任务吗？
>
> 此题和原题的不同点：
>
> 1. 每种草药可以无限制地疯狂采摘。
> 2. 药的种类眼花缭乱，采药时间好长好长啊！师傅等得菊花都谢了！
>
> ## 输入格式
>
> 输入第一行有两个整数，分别代表总共能够用来采药的时间 t 和代表山洞里的草药的数目 m。
>
> 第 2 到第 (m+1) 行，每行两个整数，第 (i+1) 行的整数 ai,bi 分别表示采摘第 i 种草药的时间和该草药的价值。
>
> ## 输出格式
>
> 输出一行，这一行只包含一个整数，表示在规定的时间内，可以采到的草药的最大总价值。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 70 3
> 71 100
> 69 1
> 1 2
> ```
>
> **输出 #1**
>
> ```
> 140
> ```
>
> ## 说明/提示
>
> #### 数据规模与约定
>
> - 对于 30% 的数据，保证 m≤1e3。
> - 对于 100% 的数据，保证 1≤m≤1e4，1≤t≤1e7，且 1≤m×t≤1e7，1≤ai,bi≤1e4。

代码如下：

```c++
#include <iostream>

using namespace std;
int weight[10005], value[10005];
long long dp[10000005];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int t, n;
    cin >> t >> n;

    for (int i = 1; i <= n; ++i) {
        cin >> weight[i] >> value[i];
    }

    for (int i = 1; i <= n; ++i) {
        for (int j = weight[i]; j <= t; ++j) {
            dp[j] = max(dp[j], dp[j - weight[i]] + value[i]);
        }
    }

    cout << dp[t];

    return 0;
}
```

#### 题目6：

> ## 题目描述
>
> 给出一个长度为 n 的序列 a，选出其中连续且非空的一段使得这段和最大。
>
> ## 输入格式
>
> 第一行是一个整数，表示序列的长度 n。
>
> 第二行有 n 个整数，第 i 个整数表示序列的第 i 个数字 ai。
>
> ## 输出格式
>
> 输出一行一个整数表示答案。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 7
> 2 -4 3 -1 2 -4 3
> ```
>
> **输出 #1**
>
> ```
> 4
> ```
>
> ## 说明/提示
>
> #### 样例 1 解释
>
> 选取 [3,5] 子段 {3,−1,2}，其和为 4。
>
> #### 数据规模与约定
>
> - 对于 40% 的数据，保证 n≤2×103。
> - 对于 100% 的数据，保证 1≤n≤2×105，−104≤ai≤104。
>
> ------
>
> 2026/01/21：增加一组 [hack 数据](https://www.luogu.com.cn/ticket/KLET103093)。

我们分析一下这道题，发现其肯定不是完全背包问题，就算使用动态规划也是应该是01背包。

我们不妨设dp数组中存储的是以此数为结尾的最大字段和，则状态转移方程如下：
$$
dp(i) = max(dp(i - 1) + a[i], a[i])
$$
甚至都不用双次循环。

代码如下：

```c++
#include <iostream>

using namespace std;
int dp[200005], a[200005];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n;
    cin >> n;

    for (int i = 1; i <= n; ++i) {
        cin >> a[i];
    }

    for (int i = 1; i <= n; ++i) {
            dp[i] = max(a[i], a[i] + dp[i - 1]);
    }
    int max = -2e6;
    for (int i = 1; i <= n; ++i) {
        if (max < dp[i])
            max = dp[i];
    }

    cout << max;

    return 0;
}
```

#### 题目7：

> ## 题目描述
>
> 这是一个简单的动规板子题。
>
> 给出一个由 n(n≤5000) 个不超过 106 的正整数组成的序列。请输出这个序列的**最长上升子序列**的长度。
>
> 最长上升子序列是指，从原序列中**按顺序**取出一些数字排在一起，这些数字是**逐渐增大**的。
>
> ## 输入格式
>
> 第一行，一个整数 n，表示序列长度。
>
> 第二行有 n 个整数，表示这个序列。
>
> ## 输出格式
>
> 一个整数表示答案。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 6
> 1 2 4 1 3 4
> ```
>
> **输出 #1**
>
> ```
> 4
> ```
>
> ## 说明/提示
>
> 分别取出 1、2、3、4 即可。

定义``dp[i]``表示以第``i``个数结尾的最长上升子序列长度。

当``a[j]``<``a[i]``时，认为可以上升，执行一遍这个状态转移方程，找到最大的或者说最长的在当前``i``情况下的子序列长度：
$$
dp(i) = max(dp(i), dp(j) + 1)
$$
只要记得去把``dp``初始化为1就可。代码如下：

```c++
#include <iostream>

using namespace std;
int dp[200005], a[5005];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n;
    cin >> n;

    for (int i = 1; i <= n; ++i) {
        cin >> a[i];
        dp[i] = 1;
    }
    int maxans = -2e6;
    for (int i = 1; i <= n; ++i) {
        for (int j = 1; j < i; ++j) {
            if (a[i] > a[j]) dp[i] = max(dp[i], dp[j] + 1);
        }
        if (dp[i] > maxans) maxans = dp[i];
    }

    cout << maxans;

    return 0;
}
```

---

