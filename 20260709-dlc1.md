上接[小学期训练#4 07-09](https://github.com/Casper233/CUGBACM-Solution/blob/main/20260709.md) 

#### 题目3：

> ## 题目描述
>
> 在大学里每个学生，为了达到一定的学分，必须从很多课程里选择一些课程来学习，在课程里有些课程必须在某些课程之前学习，如高等数学总是在其它课程之前学习。现在有 N 门功课，每门课有若干学分，分别记作 s1,s2,⋯,sN，每门课有一门或没有直接先修课（若课程 a 是课程 b 的先修课即只有学完了课程 a，才能学习课程 b）。一个学生要从这些课程里选择 M 门课程学习，问他能获得的最大学分是多少？
>
> 题目保证课程安排无冲突。（即不会有 a 是 b 的先修课，b 也是 a 的先修课这类情况存在。）
>
> ## 输入格式
>
> 第一行有两个整数 N，M 用空格隔开 (1≤N≤300 , 1≤M≤300)。
>
> 接下来的 N 行，第 i+1 行包含两个整数 ki 和 si，ki 表示第 i 门课的直接先修课，si 表示第 i 门课的学分。若 ki=0 表示没有直接先修课 (0≤ki≤N,1≤si≤20)。
>
> 数据保证至少存在一个 ki=0，即至少一门课无先修课。
>
> ## 输出格式
>
> 只有一行，选 M 门课程的最大学分。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 7 4
> 2 2
> 0 1
> 0 4
> 2 1
> 7 1
> 7 6
> 2 2
> ```
>
> **输出 #1**
>
> ```
> 13
> ```

每门课最多只有一门先修课，我们可以根据其进行建树，设一门0学分的0号课程，作为所有的无先修课的的先修课。

设``dp[u][j]``表示在以``u``为根的子树中，选择``j``门课程获得的最大学分。

有
$$
dp[u][j + k] = max(dp[u][j + k], dp[u][j] + dp[v][k])
$$
代码如下：

```c++
#include <iostream>
#include <vector>
using namespace std;

int n = 3005, m = 3005;
vector<int> score(n + 1, 0);
vector<vector<int>> cnt(n + 1);
vector<vector<int>> dp(n + 1, vector<int>(m + 1, -1));
vector<int> temvec(n + 1, 0);

void dfs(int u){
    if (u != 0) {
        if (m >= 1) dp[u][1] = score[u];
        dp[u][0] = 0;
        temvec[u] = 1;
    } else {
        dp[u][0] = 0;
        temvec[u] = 0;
    }

    for (int v : cnt[u]) {
        dfs(v);

        int tempa = min(temvec[u], m);
        int tempb = min(temvec[v], m);

        for (int j = tempa; j >= (u == 0 ? 0 : 1); j--) {
            if (dp[u][j] == -1) continue;
            for (int k = 1; k <= tempb && j + k <= m; k++) {
                if (dp[v][k] == -1) continue;
                dp[u][j + k] = max(dp[u][j + k], dp[u][j] + dp[v][k]);
            }
        }

        temvec[u] += temvec[v];
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);


    cin >> n >> m;

    for (int i = 1; i <= n; i++) {
        int k;
        cin >> k >> score[i];
        cnt[k].push_back(i);
    }


    dfs(0);

    cout << dp[0][m];

    return 0;
}
```

### 1.3 换根DP

树形 DP 中的换根 DP 问题又被称为二次扫描，通常不会指定根结点，并且根结点的变化会对一些值，例如子结点深度和、点权和等产生影响。

通常需要两次 DFS，第一次 DFS 预处理诸如深度，点权和之类的信息，在第二次 DFS 开始运行换根动态规划。

---

## 二、状压DP

状压DP，全称状态压缩DP，是动态规划的一种，通过将状态集合转化为整数记录在DP状态中来实现状态转移的目的。

为了达到更低的时间复杂度，通常需要寻找更低状态数的状态。大部分题目中会利用二元状态，用``n``位二进制数表示``n``个独立二元状态的情况。

状压 DP 适合处理：

- 数据规模小

- 每个位置有选或不选

- 状态之间限制复杂

状压 DP 的基本流程

1. 枚举所有状态
2. 判断每个状态是否合法
3. 预处理两个状态能否相邻
4. 设计 DP 数组
5. 按行或按层转移

#### 题目6：

> ## 题目描述
>
> 在 N×N 的棋盘里面放 K 个国王，使他们互不攻击，共有多少种摆放方案。国王能攻击到它上下左右，以及左上左下右上右下八个方向上附近的各一个格子，共 8 个格子。
>
> ## 输入格式
>
> 只有一行，包含两个数 N,K。
>
> ## 输出格式
>
> 所得的方案数。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 3 2
> ```
>
> **输出 #1**
>
> ```
> 16
> ```
>
> ## 说明/提示
>
> ### 数据范围及约定
>
> 对于全部数据，1≤N≤9，0≤K≤N×N。
>
> ------
>
> upd 2018.4.25：数据有加强。

由于每个格子只有两种状态：放国王 和 不放国王 ，所以我们可以用 1 表示放国王，0 表示不放国王。我们可以将其以二进制的形式存储，再在10进制的情况下记录到变量中。

所以状压dp的套路就是不断的去枚举表示状态的数，去转移即可。

状态设计：

设\( dp[i][s][k] \)表示

- 已经处理完前 \( i \) 行，
- 第 \( i \) 行的状态为 \( s \)
- 目前总共放置了 \( k \) 个国王，
  的方案总数。

预处理是枚举所有 \( 0 \sim (1<<N)-1 \) 的二进制数 ``i``，若满足 ``(i & (i << 1)) == 0``，则 ``i`` 是一个行内合法的状态。将其存入 ``sta`` 数组，并计算其二进制中 1 的个数 ``cnt`` 存入 ``ccnta``。

对于任意两个合法状态 ``a`` 和 ``b``，若同时满足：

- ``(a & b) == 0``（正下方无国王）
- ``(a & (b << 1)) == 0``（左下方无国王）
- ``(a & (b >> 1)) == 0``（右下方无国王）
  则状态 ``a`` 可以转移到状态 ``b``（即 ``a`` 在上一行，``b`` 在当前行）。

初始化``dp[0][0][0] = 1``。

对于每个上一行状态编号 `p` 和当前行状态编号 `c`，如果 `(p, c)` 是合法转移，则：有
$$
dp[i][c][k + ccnta[c]] += dp[i-1][p][k]
$$
其中 \( k \) 从 0 到 \( K - kingCnt[c] \)。

代码如下：

```c++
#include <iostream>
#include <vector>
using namespace std;

int count(int x) {
    int cnt = 0;
    while (x) {
        cnt++;
        x &= x - 1;
    }
    return cnt;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, m;
    cin >> n >> m;

    vector<int> sta;
    vector<int> ccnta;
    for (int i = 0; i < (1 << n); ++i) {
        if (i & (i << 1)) continue;
        sta.push_back(i);
        ccnta.push_back(count(i));
    }

    int temp = sta.size();

    vector<vector<int>> transfer(temp);
    for (int p = 0; p < temp; ++p) {
        for (int c = 0; c < temp; ++c) {
            int tempa = sta[p];
            int tempb = sta[c];
            if ((tempa & tempb) == 0 &&
                (tempa & (tempb << 1)) == 0 &&
                (tempa & (tempb >> 1)) == 0) {
                transfer[p].push_back(c);
                }
        }
    }

    vector dp(temp, vector<long long>(m + 1, 0));
    dp[0][0] = 1;

    for (int i = 1; i <= n; ++i) {
        vector ndp(temp, vector<long long>(m + 1, 0));
        for (int p = 0; p < temp; ++p) {
            for (int c : transfer[p]) {
                int cnt = ccnta[c];
                for (int k = 0; k + cnt <= m; ++k) {
                    if (dp[p][k] == 0) continue;
                    ndp[c][k + cnt] += dp[p][k];
                }
            }
        }
        dp.swap(ndp);
    }

    long long ans = 0;
    for (int c = 0; c < temp; ++c) {
        ans += dp[c][m];
    }
    cout << ans;

    return 0;
}
```

---

## 三、数位 DP

数位 DP ⽤于统计区间内数字的性质。

求 [L, R] 的个数，通常转化为``solve(R) - solve(L-1)``。我们只需要设计一个函数``dfs(pos, ...)``，它能求出``[0, X]``区间内满足条件的数字个数。这样一来，问题就聚焦在了“如何统计不超过上界X的数”。

他有三个参数一般。

**``pos`` (当前位置)**：从高位往低位递归，`pos=0` 表示个位，`pos=len-1` 表示最高位。

**``limit`` (是否受限)**：布尔值。表示我们当前构造的前缀是否和上界 X 的前缀完全一致。

- 若``limit=True``，当前位最大只能取``X``的当前位数字；
- 若``limit=False``，当前位可以任意取``0~9``。

**``lead`` (前导零)**：布尔值。表示当前位是否还在前导零状态（即数字尚未开始）。

一种实现的模板如下：

```c++
#include <cstdio>
#include <cstring>
#include <iostream>
using namespace std;
using ll = long long;
constexpr int N = 50005;
ll a, b;
ll f[15], ksm[15], p[15], now[15];

ll dfs(int u, int x, bool f0,
       bool lim) {  // u 表示位数，f0 是否有前导零，lim 是否都贴在上限上
  if (!u) {
    if (f0) f0 = false;
    return 0;
  }
  if (!lim && !f0 && (~f[u])) return f[u];
  ll cnt = 0;
  int lst = lim ? p[u] : 9;
  for (int i = 0; i <= lst; i++) {  // 枚举这位要填的数字
    if (f0 && i == 0)
      cnt += dfs(u - 1, x, 1, lim && i == lst);  // 处理前导零
    else if (i == x && lim && i == lst)
      cnt += now[u - 1] + 1 +
             dfs(u - 1, x, 0,
                 lim && i == lst);  // 此时枚举的前几位都贴在给定的上限上．
    else if (i == x)
      cnt += ksm[u - 1] + dfs(u - 1, x, 0, lim && i == lst);
    else
      cnt += dfs(u - 1, x, 0, lim && i == lst);
  }
  if ((!lim) && (!f0)) f[u] = cnt;  // 只有不贴着上限和没有前导零才能记忆
  return cnt;
}

ll gans(ll d, int dig) {
  int len = 0;
  memset(f, -1, sizeof(f));
  while (d) {
    p[++len] = d % 10;
    d /= 10;
    now[len] = now[len - 1] + p[len] * ksm[len - 1];
  }
  return dfs(len, dig, 1, 1);
}

int main() {
  scanf("%lld%lld", &a, &b);
  ksm[0] = 1;
  for (int i = 1; i <= 12; i++) ksm[i] = ksm[i - 1] * 10ll;
  for (int i = 0; i < 9; i++) printf("%lld ", gans(b, i) - gans(a - 1, i));
  printf("%lld\n", gans(b, 9) - gans(a - 1, 9));
  return 0;
}
```

