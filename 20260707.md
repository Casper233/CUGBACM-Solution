 

# CUGBACM25级暑假小学期训练 #2 数据结构

今天是什么？今天是数据结构。

## 一、栈

### 1.1 栈

奇妙的线性结构，STL实现的一部分，遵循的机制是先进后出FILO（或者是后进先出LIFO）。数据的增添之类的操作只能在栈的顶部进行，无法在栈头部或者其他地方进行。

部分STL提供的实现的函数如下：

```c++
#include <stack>

stact<int> sta;

sta.push(elem); //放入栈elem元素
sta.pop(); //删除栈顶第一个元素
sta.top(); //返回栈顶第一个元素
sta.empty(); //返回栈是否为空
sta.size(); //返回栈大小
```

#### 题目1：

> # B3614 【模板】栈
>
> ## 题目描述
>
> 请你实现一个栈（stack），支持如下操作：
>
> - `push(x)`：向栈中加入一个数 x。
> - `pop()`：将栈顶弹出。如果此时栈为空则不进行弹出操作，输出 `Empty`。
> - `query()`：输出栈顶元素，如果此时栈为空则输出 `Anguei!`。
> - `size()`：输出此时栈内元素个数。
>
> ## 输入格式
>
> **本题单测试点内有多组数据**。  
> 输入第一行是一个整数 T，表示数据组数。对于每组数据，格式如下：  
> 每组数据第一行是一个整数，表示操作的次数 n。  
> 接下来 n 行，每行首先由一个字符串，为 `push`，`pop`，`query` 和 `size` 之一。若为 `push`，则其后有一个整数 x，表示要被加入的数，x 和字符串之间用空格隔开；若不是 `push`，则本行没有其它内容。
>
> ## 输出格式
>
> 对于每组数据，按照「题目描述」中的要求依次输出。每次输出占一行。
>
> ## 输入输出样例 #1
>
> ### 输入 #1
>
> ```
> 2
> 5
> push 2
> query
> size
> pop
> query
> 3
> pop
> query
> size
> ```
>
> ### 输出 #1
>
> ```
> 2
> 1
> Anguei!
> Empty
> Anguei!
> 0
> ```
>
> ## 说明/提示
>
> ### 样例 1 解释
>
> 对于第二组数据，始终为空，所以 `pop` 和 `query` 均需要输出对应字符串。栈的 size 为 0。
>
> ### 数据规模与约定
>
> 对于全部的测试点，保证 1≤T,n≤1e6，且单个测试点内的 n 之和不超过 1e6，即 ∑n≤1e6。保证 0≤x<2^64。
>
> ### 提示
>
> - 请注意大量数据读入对程序效率造成的影响。
> - 请注意输出的 `Empty` 不含叹号，`Anguei!` 含有叹号。

本题是一道简单的模板题，加上一个字符串识别然后维护一个stack即可。

本题唯一需要注意的点为x的范围，2^64可能需要我们使用``unsigned long long``，其他没有什么坑~~应该~~。

代码如下：

```c++
#include <iostream>
#include <string>
#include <stack>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int T;
    cin >> T;
    for (int i = 0; i < T; ++i) {
        int n;
        stack<unsigned long long> sta;
        cin >> n;
        for (int j = 0; j < n; ++j) {
            string action;
            cin >> action;
            if (action == "push") {
                unsigned long long x;
                cin >> x;
                sta.push(x);
            }
            else if (action == "pop") {
                if (sta.empty()) cout << "Empty" << '\n';
                else {
                    sta.pop();
                }
            }
            else if (action == "query") {
                if (sta.empty()) cout << "Anguei!" << '\n';
                else {
                    cout << sta.top() << '\n';
                }
            }
            else if (action == "size") {
                cout << sta.size() << '\n';
            }
        }
    }
    return 0;
}
```

---

### 1.2 单调栈

何为单调栈？就是满足单调增/减的一个栈结构。和单调队列（详见2.4）不同的是，他只能在一段进行输入输出。遍历数组时，找大/小于每个元素时的时间复杂度为O(n)。

单调栈的维护过程如下：

在创建完栈后，当需要插入一个元素时，为了维护栈的单调性，我们需要保证插入元素后栈满足单调性，并且弹出的元素最少。

如果有一个栈，其中自顶而下为``{1, 3, 9, 17}``。插入元素``8``。

- 首先为了保证单调性，需要弹出``1``,``3``。栈变成``{9, 17}``。
- 插入``8``，栈变为``{8, 9, 17}``。
- 再移入``3``,``1``。栈变为``{1, 3, 8, 9, 17}``。

实现的代码如下：

```c++
vector<int> nextGreaterElementIndex(const vector<int>& arr) {
    int n = arr.size();
    vector<int> res(n, -1);
    if (n == 0) return res;
    
    stack<int> st;
    st.push(0);
    for (int i = 1; i < n; ++i) {
        while (!st.empty() && arr[i] > arr[st.top()]) {
            res[st.top()] = i;
            st.pop();
        }
        st.push(i);
    }
    return res;
}
```

#### 题目2：

> # P5788 【模板】单调栈
>
> ## 题目背景
>
> 模板题，无背景。  
>
> 2019.12.12 更新数据，放宽时限，现在不再卡常了。
>
> ## 题目描述
>
> 给出项数为 n 的整数数列 a1…n。
>
> 定义函数 f(i) 代表数列中第 i 个元素之后第一个大于 ai 的元素的**下标**，即 f(i)=mini<j≤n,aj>ai{j}。若不存在，则 f(i)=0。
>
> 试求出 f(1…n)。
>
> ## 输入格式
>
> 第一行一个正整数 n。
>
> 第二行 n 个正整数 a1…n。
>
> ## 输出格式
>
> 一行 n 个整数表示 f(1),f(2),…,f(n) 的值。
>
> ## 输入输出样例 #1
>
> ### 输入 #1
>
> ```
> 5
> 1 4 2 3 5
> 
> ```
>
> ### 输出 #1
>
> ```
> 2 5 4 5 0
> 
> ```
>
> ## 说明/提示
>
> 【数据规模与约定】
>
> 对于 30% 的数据，n≤100；
>
> 对于 60% 的数据，n≤5×103 ；
>
> 对于 100% 的数据，1≤n≤3×106，1≤ai≤109。

代码如下：

```c++
#include <iostream>
#include <vector>
#include <stack>

using namespace std;

long long ans[3000005];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);


    int n;
    cin >> n;
    vector<long long> que;
    stack<long long> sta;
    long long a;
    for (int i = 0; i < n; ++i) {
        cin >> a;
        que.push_back(a);
    }
    for (int i = n; i > 0; --i) {
        while (!sta.empty() && que[sta.top()] <= que[i-1]) sta.pop();
        ans[i] = (!sta.empty()? sta.top() + 1 : 0);
        sta.push(i-1);
    }
    for (int i = 1; i <= n; ++i)
        cout << ans[i] << " ";
    return 0;
}
```

---

## 二、队列

### 2.1 队列

队列，queue。与栈相比，区别在于队列遵循先进先出(FIFO)，而非后进先出。

部分STL提供的实现如下：

```c++
#include <queue>

queue<int> que;
que.push(x); //向队尾添加元素x
que.pop(); //删除队首第一个元素
int val = que.front(); //获取队首元素
```

#### 题目3：

> ## 题目描述
>
> 请你实现一个队列（queue），支持如下操作：
>
> - `push(x)`：向队列中加入一个数 x。
> - `pop()`：将队首弹出。如果此时队列为空，则不进行弹出操作，并输出 `ERR_CANNOT_POP`。
> - `query()`：输出队首元素。如果此时队列为空，则输出 `ERR_CANNOT_QUERY`。
> - `size()`：输出此时队列内元素个数。
>
> ## 输入格式
>
> 第一行，一个整数 n，表示操作的次数。
>
> 接下来 n 行，每行表示一个操作。格式如下：
>
> - `1 x`，表示将元素 `x` 加入队列。
> - `2`，表示将队首弹出队列。
> - `3`，表示查询队首。
> - `4`，表示查询队列内元素个数。
>
> ## 输出格式
>
> 输出若干行，对于每个操作，按「题目描述」输出结果。
>
> 每条输出之间应当用空行隔开。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 13
> 1 2
> 3
> 4
> 1 233
> 3
> 2
> 3
> 2
> 4
> 3
> 2
> 1 144
> 3
> ```
>
> **输出 #1**
>
> ```
> 2
> 1
> 2
> 233
> 0
> ERR_CANNOT_QUERY
> ERR_CANNOT_POP
> 144
> ```
>
> ## 说明/提示
>
> ### 样例解释
>
> 首先插入 `2`，队首为 `2`、队列内元素个数为 `1`。
>  插入 `233`，此时队首为 `2`。
>  弹出队首，此时队首为 `233`。
>  弹出队首，此时队首为空。
>  再次尝试弹出队首，由于队列已经为空，此时无法弹出。
>  插入 `144`，此时队首为 `144`。
>
> ### 数据规模与约定
>
> 对于 100% 的测试数据，满足 n≤10000，且被插入队列的所有元素值是 [1,1000000] 以内的正整数。

跟题目1一样，都是比较简单的模板题。代码如下：

```c++
#include <iostream>
#include <queue>

using namespace std;

long long ans[3000005];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, sel;
    cin >> n;

    queue<int> que;

    for (int i = 0; i < n; ++i) {
        cin >> sel;
        if (sel == 1) {
            int temp;
            cin >> temp;
            que.push(temp);
            continue;
        }
        if (sel == 2) {
            if (que.empty()) {
                cout << "ERR_CANNOT_POP" << '\n';
                continue;
            }
            que.pop();
            continue;
        }
        if (sel == 3) {
            if (que.empty()) {
                cout << "ERR_CANNOT_QUERY" << '\n';
                continue;
            }
            cout << que.front() << '\n';
            continue;
        }
        if (sel == 4) {
            cout << que.size() << '\n';
            continue;
        }
    }

    return 0;
}
```

---

### 2.2 优先队列

优先队列，Priority Queue。是一种特殊的队列，通常底层由堆(Heap)完成实现，按元素的优先级进行排序。默认的优先级为从堆底到堆顶由小到大。STL实现的调用方法如下：

```c++
#include <queue>

priority_queue<int> que;
//priority_queue<int, vector<int>, greater<int>> 小顶堆 需要显式定义容器与比较器

que.push(x); //插入元素 重新排堆
que.top(); //获取堆顶元素
que.pop(); //删除队首第一个元素
```

注：在优先队列中，队首与堆顶所指的为同一个元素，在普通队列中无堆顶，普通堆中无队首。

---

### 2.3 双端队列

双端队列，double-ended queue，是一种具有队列，同时也有栈性质的数据结构。可以同时模拟先进后出和先进先出。能够提供线性复杂度的插入和删除，以及常数复杂度的随机访问。

使用STL实现的方法如下：

```c++
#include <deque>

deque<int> dq;
dq.front() //返回队头
dq.back() //返回队尾
dq.pop_back() //删除队尾，不返回值
dq.pop_front() //删除队头，不返回值
dq.push_back(x) //在队尾添加一个元素 x
dq.push_front(x) //在队头添加一个元素 x
dq.at() //返回容器中指定位置元素的引用，执行越界检查，常数复杂度．
operator[] //返回容器中指定位置元素的引用．不执行越界检查，常数复杂度．
```

其原理是使用一段一段定量内存空间，无论是填充满还是只有一个元素。其内存使用量显著大于``链表``（``list``）。

如下的题目就是一个可以同时使用deque和list的例子，但是我们会发现deque会报MLE。~~还是我的问题？~~

#### 题目4：

> # B3656 【模板】双端队列 1
>
> ## 题目背景
>
> Aya 衷心祝愿大家不再因为 `std::deque` 重蹈覆辙。
>
> ## 题目描述
>
> 请你实现 m 个双端队列，支持如下的 q 次操作：
>
> - 请你实现 m 个双端队列，支持如下的 q 次操作：
>
>   - `push_back(a,x)`：在第 a 个双端队列中从尾部插入一个元素 x；
>   - `pop_back(a)`：在第 a 个双端队列中从尾部弹出一个元素。
>   - `push_front(a,x)`：在第 a 个双端队列中从头部插入一个元素 x；
>   - `pop_front(a)`：在第 a 个双端队列中从头部弹出一个元素。
>   - `size(a)`：查询第 a 个双端队列的元素个数；
>   - `front(a)`：查询第 a 个双端队列的队首元素；
>   - `back(a)`：查询第 a 个双端队列的队尾元素；
>
>   对于 `pop_back`，`pop_front`，`front` 和 `back` 操作，若当前双端队列为空则不进行，直接跳过该次操作。
>
> ## 输入格式
>
> 输入的第一行是一个正整数 q，表示操作次数。
>
> 接下来 q 行，每行先是一个字符串，保证为 `push_back` 或者 `pop_back` 或者 `push_front` 或者 `pop_front` 或者 `size` 或者 `front` 或者 `back` 之一。接下来是 1 或 2 个正整数，分别表示 a 和 x。
>
> ## 输出格式
>
> 对于每个 `size` 或者 `front` 或者 `back` 操作，输出一行表示答案。
>
> ## 输入输出样例 #1
>
> ### 输入 #1
>
> ```
> 10
> pop_back 2
> push_back 1 1
> push_front 1 3
> push_front 2 2
> push_front 2 3
> pop_back 1
> size 1
> push_back 2 3
> back 1
> front 1
> ```
>
> ### 输出 #1
>
> ```
> 1
> 3
> 3
> 
> ```
>
> ## 说明/提示
>
> **【数据范围】**
>
> | 子任务 |  m≤  |  q≤  | 分值 |
> | :----: | :--: | :--: | :--: |
> |   1    |  10  |  10  |  10  |
> |   2    | 2000 | 2000 |  20  |
> |   3    | 1e5  | 1e5  |  30  |
> |   4    | 1e6  | 1e6  |  40  |
>
> 对于所有数据，1≤m,q≤1e6，1≤x≤1e9，1≤a≤1e6

我们创建一个``deque``的数组，然后编辑就行了，就酱。

然后就丝滑的全都MLE了 =/  ~~那我们直接回宿舍睡觉去吧~~

我们分析后发现这个数据量给deque有点强人所难了（

让我们来把deque换成list就好啦。代码如下：

```c++
#include <iostream>
#include <list>

using namespace std;

list<long long> li[1000005];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);


    int m;
    cin >> m;
    string input;
    for (int i = 0; i < m; i++) {
        cin >> input;
        if (input == "push_back") {
            long long a, b;
            cin >> a >> b;
            li[a].push_back(b);
            continue;
        }
        if (input == "pop_back") {
            long long a;
            cin >> a;
            if (li[a].empty()) continue;
            li[a].pop_back();
            continue;
        }
        if (input == "push_front") {
            long long a, b;
            cin >> a >> b;
            li[a].push_front(b);
            continue;
        }
        if (input == "pop_front") {
            long long a;
            cin >> a;
            if (li[a].empty()) continue;
            li[a].pop_front();
            continue;
        }
        if (input == "size") {
            long long a;
            cin >> a;
            cout << li[a].size() << '\n';
            continue;
        }
        if (input == "front") {
            long long a;
            cin >> a;
            if (li[a].empty()) continue;
            cout << li[a].front() << '\n';
            continue;
        }
        if (input == "back") {
            long long a;
            cin >> a;
            if (li[a].empty()) continue;
            cout << li[a].back() << '\n';
            continue;
        }
    }

    return 0;
}
```

~~但是这个题真的要放在这里吗（困惑）~~

---

### 2.4 单调队列

单调队列，Monotonic Queue，顾名思义就是满足单调增/减的一个队列结构。~~（咋感觉在哪里写过这段话）~~然后可以用于解决类似滑动窗口的那些问题。我们用题来看吧。

#### 题目5：

> ## 题目描述
>
> 有一个长为 n 的序列 a，以及一个大小为 k 的窗口。现在这个窗口从左边开始向右滑动，每次滑动一个单位，求出每次滑动后窗口中的最小值和最大值。
>
> 例如，对于序列 [1,3,−1,−3,5,3,6,7] 以及 k=3，有如下过程：
>
> |            窗口位置            | 最小值 | 最大值 |
> | :----------------------------: | :----: | :----: |
> | [1   3  -1] -3   5   3   6   7 |   -1   |   3    |
> | 1  [3  -1  -3]  5   3   6   7  |   -3   |   3    |
> | 1   3 [-1  -3   5]  3   6   7  |   -3   |   5    |
> | 1   3  -1 [-3   5   3]  6   7  |   -3   |   5    |
> | 1   3  -1  -3  [5   3   6]  7  |   3    |   6    |
> | 1   3  -1  -3   5  [3   6   7] |   3    |   7    |
>
> ## 输入格式
>
> 输入一共有两行，第一行有两个正整数 n,k；
> 第二行有 n 个整数，表示序列 a。
>
> ## 输出格式
>
> 输出共两行，第一行为每次窗口滑动的最小值；
> 第二行为每次窗口滑动的最大值。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 8 3
> 1 3 -1 -3 5 3 6 7
> ```
>
> **输出 #1**
>
> ```
> -1 -3 -3 -3 3 3
> 3 3 5 5 6 7
> ```
>
> ## 说明/提示
>
> 【数据范围】
> 对于 50% 的数据，1≤n≤1e5；
> 对于 100% 的数据，1≤k≤n≤1e6，ai​∈[−2^31,2^31)。

我们可以维护一个单调递减和一个单调递增数列，分别来求最大值和最小值。

我们以最大值为例：首先我们维护一个单调递减队列。

```c++
int le = 1; ri = 0;

while (le <= ri && input[que[ri]] <= input[i]) ri --;
    ri ++;
    que[ri] = i;
```

这里面会把 ri 移到与i相比第一项比i大的仍在 le 包含的值的位置。其他的就不要了，因为他们再也不可能是最大值了。

同时我们要控制左侧边界右移动。

```c++
while (que[le] <= i - k && le <= ri) le ++;
```

窗口范围：`(i−k , i]`

下标 ≤ i−k 代表已经滑出窗口左侧，就算它是队列里最大的，也不在当前窗口，要弹出队首。

最后加入在窗口里有3个元素再进行输出即可。

同理可得最小值。揉一块，得到代码如下：

```c++
#include <iostream>
#include <vector>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, k;
    cin >> n >> k;
    int le = 1, ri = 0;
    vector<long long> input(1000005);
    vector<long long> que(1000005);
    for (int i = 1; i <= n; ++i) {
        cin >> input[i];
    }

    for (int i = 1; i <= n; ++i) {
        while (le <= ri && input[que[ri]] >= input[i]) ri --;
        ri ++;
        que[ri] = i;
        while (que[le] <= i - k && le <= ri) le ++;
        if (i >= k) {
           cout << input[que[le]] << ' ';
        }
    }
    cout << '\n';
    le = 1, ri = 0;
    for (int i = 1; i <= n; ++i) {
        while (le <= ri && input[que[ri]] <= input[i]) ri --;
        ri ++;
        que[ri] = i;
        while (que[le] <= i - k && le <= ri) le ++;
        if (i >= k) {
            cout << input[que[le]] << ' ';
        }
    }

    return 0;
}
```

---

## 三、并查集

### 3.1 并查集

并查集是一种用于管理元素所属集合的数据结构，实现为一个森林，其中每棵树表示一个集合，树中的节点表示对应集合中的元素。

顾名思义，并查集支持两种操作：

- 合并（Union）：合并两个元素所属集合（合并对应的树）。
- 查询（Find）：查询某个元素所属集合（查询对应的树的根节点），这可以用于判断两个元素是否属于同一集合。

并查集高效处理集合的合并和查询。简而言之，他的原理可以如下表示。

假设我们有许多不同的集合，[1, 2, 3] [4, 5] [6]。其中每个的第一项都是其最上面一层的节点，他们指向自己作为父节点，剩下的元素都指向其作为父节点。

我们可以通过使用一个数组，来记录其父节点。这个也就是并查集的数据结构。

``Find``操作就是找其父节点是否是相同根节点就可。

``Union``操作就是把两个树中的一个根节点的父节点设为另外一个节点的根节点。就酱紫。

作为一种算法，他有许多比较成熟的模板。

```c++
// 初始化
void Init(int n, int &p[]) {
    for (int i = 0; i < n; ++i) {
        p[i] = i;
    }
}

// 搜索
int Find(int x, int &p[]) {
	if (x == p[x]) return x;
    return Find(p[x], p);
}

// 搜索+路径压缩
int Find(int x, int &p[]) {
	if (x == p[x]) return x;
    return p[x] = Find(p[x], p);
}

//合并 x -> y (y的根节点为最后的根节点)
void Union(int x, int y, int &p[]) {
    int rootx = Find(x, p);
    int rooty = Find(y, p);
    if (rootx != rooty){
        p[rootx] = rooty
    }
}
```

还有许多不同的实现方法 ~~但是我喜欢这种~~

#### 题目6：

> ## 题目背景
>
> **本题数据范围已经更新到 1≤N≤2×1e5，1≤M≤1e6。**
>
> ## 题目描述
>
> 如题，现在有一个并查集，你需要完成合并和查询操作。
>
> ## 输入格式
>
> 第一行包含两个整数 N,M ,表示共有 N 个元素和 M 个操作。
>
> 接下来 M 行，每行包含三个整数 Zi,Xi,Yi 。
>
> 当 Zi=1 时，将 Xi 与 Yi 所在的集合合并。
>
> 当 Zi=2 时，输出 Xi 与 Yi 是否在同一集合内，是的输出 `Y` ；否则输出 `N` 。
>
> ## 输出格式
>
> 对于每一个 Zi=2 的操作，都有一行输出，每行包含一个大写字母，为 `Y` 或者 `N` 。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 4 7
> 2 1 2
> 1 1 2
> 2 1 2
> 1 3 4
> 2 1 4
> 1 2 3
> 2 1 4
> ```
>
> **输出 #1**
>
> ```
> N
> Y
> N
> Y
> ```
>
> ## 说明/提示
>
> 对于 15% 的数据，N≤10，M≤20。
>
> 对于 35% 的数据，N≤100，M≤103。
>
> 对于 50% 的数据，1≤N≤104，1≤M≤2×105。
>
> 对于 100% 的数据，1≤N≤2×105，1≤M≤106，1≤Xi,Yi≤N，Zi∈{1,2}。

本题还是相对比较简单的，只要理解了并查集的实现即可完成。

代码如下：

```c++
#include <iostream>
#include <vector>

using namespace std;

int find(int a, vector<int> &p);

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, m;
    cin >> n >> m;

    vector<int> p(200005);
    //init
    for (int i = 1; i <= n; ++i) {
        p[i] = i;
    }

    for (int i = 0; i < m; ++i) {
        int a, b, c;
        cin >> a >> b >> c;
        if (a == 1) {
            //hebing
            int rootb, rootc;
            rootb = find(b, p);
            rootc = find(c, p);
            p[rootb] = rootc;
            continue;
        }
        if (a == 2) {
            if (find(b, p) == find(c, p)) cout << "Y" << '\n';
            else cout << "N" << '\n';
        }
    }

    return 0;
}

int find(int a, vector<int> &p) {
    if (a == p[a]) return a;
    return p[a] = find(p[a], p);
}
```

#### 题目7：

> ## 题目描述
>
> 若某个家族人员过于庞大，要判断两个是否是亲戚，确实还很不容易，现在给出某个亲戚关系图，求任意给出的两个人是否具有亲戚关系。
>
> 规定：x 和 y 是亲戚，y 和 z 是亲戚，那么 x 和 z 也是亲戚。如果 x，y 是亲戚，那么 x 的亲戚都是 y 的亲戚，y 的亲戚也都是 x 的亲戚。
>
> ## 输入格式
>
> 第一行：三个整数 n,m,p，（n,m,p≤5000），分别表示有 n 个人，m 个亲戚关系，询问 p 对亲戚关系。
>
> 以下 m 行：每行两个数 Mi，Mj，1≤Mi, Mj≤n，表示 Mi 和 Mj 具有亲戚关系。
>
> 接下来 p 行：每行两个数 Pi,Pj，询问 Pi 和 Pj 是否具有亲戚关系。
>
> ## 输出格式
>
> p 行，每行一个 `Yes` 或 `No`。表示第 i 个询问的答案为“具有”或“不具有”亲戚关系。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 6 5 3
> 1 2
> 1 5
> 3 4
> 5 2
> 1 3
> 1 4
> 2 3
> 5 6
> ```
>
> **输出 #1**
>
> ```c++
> Yes
> Yes
> No
> ```

本题同上题，都是理解并查集的原理即可。

代码如下：

```c++
#include <iostream>
#include <vector>

using namespace std;

int find(int a, vector<int> &p);

void uni(int a, int b, vector<int> &p);

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, m, p;
    cin >> n >> m >> p;

    vector<int> q(200005);
    //init
    for (int i = 1; i <= n; ++i) {
        q[i] = i;
    }

    for (int i = 0; i < m; ++i) {
        int tempa, tempb;
        cin >> tempa >> tempb;
        uni(tempa, tempb, q);
    }

    for (int i = 0; i < p; ++i) {
        int tempc, tempd;
        cin >> tempc >> tempd;
        if (find(tempc, q) == find(tempd, q)) {
            cout << "Yes" <<  '\n';
        }
        else {
            cout << "No" << '\n';
        }
    }

    return 0;
}

int find(int a, vector<int> &p) {
    if (a == p[a]) return a;
    return p[a] = find(p[a], p);
}

void uni(int a, int b, vector<int> &p){
    //hebing
    int rootb, roota;
    rootb = find(b, p);
    roota = find(a, p);
    p[rootb] = roota;
}
```

#### 题目8：

> ## 题目描述
>
> 动物王国中有三类动物 A,B,C，这三类动物的食物链构成了有趣的环形。A 吃 B，B 吃 C，C 吃 A。
>
> 现有 N 个动物，以 1∼N 编号。每个动物都是 A,B,C 中的一种，但是我们并不知道它到底是哪一种。
>
> 有人用两种说法对这 N 个动物所构成的食物链关系进行描述：
>
> - 第一种说法是 `1 X Y`，表示 X 和 Y 是同类。
> - 第二种说法是 `2 X Y`，表示 X 吃 Y。
>
> 此人对 N 个动物，用上述两种说法，一句接一句地说出 K 句话，这 K 句话有的是真的，有的是假的。当一句话满足下列三条之一时，这句话就是假话，否则就是真话。
>
> - 当前的话与前面的某些真的话冲突，就是假话；
> - 当前的话中 X 或 Y 比 N 大，就是假话；
> - 当前的话表示 X 吃 X，就是假话。
>
> 你的任务是根据给定的 N 和 K 句话，输出假话的总数。
>
> ## 输入格式
>
> 第一行两个整数，N,K，表示有 N 个动物，K 句话。
>
> 第二行开始每行一句话。格式见题目描述与样例。
>
> ## 输出格式
>
> 一行，一个整数，表示假话的总数。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 100 7
> 1 101 1
> 2 1 2
> 2 2 3
> 2 3 3
> 1 1 3
> 2 3 1
> 1 5 5
> ```
>
> **输出 #1**
>
> ```
> 3
> ```
>
> ## 说明/提示
>
> 对于全部数据，1≤N≤5×1e4，1≤K≤1e5。
>
> ∣X∣,∣Y∣<2^32。

本题需要我们构建3个树，相互不矛盾，并且得出最后矛盾的数量。

我们可以定义三个情况，由 [1, n] [n+1, 2n] [2n+1, 3n]三部分构成。

然后我们可以规定：

域 1（索引 `x`）：`x` 是 A 类

域 2（索引 `x + n`）：`x` 是 B 类

域 3（索引 `x + 2n`）：`x` 是 C 类

即有“x 吃 y”:

A吃B → 合并 `x` 与 `y+n`

B吃C → 合并 `x+n` 与 `y+2n`

C吃A → 合并 `x+2n` 与 `y`

“y 吃 x”:

 合并 `y` 与 `x+n`，即 `x+n` 与 `y`，同时也会导致 `x` 与 `y+2n` 被合并。

所以判断“x 吃 y”是否为假时：

- 同类冲突：`indiff(x, y)`
- y 吃 x 冲突：`indiff(x, y + 2 * n)`（因为 `y+2n` 和 `x` 在同一集合意味着 C 吃 A 的关系已建立，也就是 y 吃 x）

综上，代码如下：

```c++
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int find(int a, vector<int> &p);

void uni(int a, int b, vector<int> &p);

bool indiff(int a, int b, vector<int> &p);

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, k;
    cin >> n >> k;
    vector<int> q(150005);
    int ans = 0;
    //init
    for (int i = 1; i <= n * 3; ++i) {
        q[i] = i;
    }

    for (int i = 1; i <= k; ++i) {
        int a, b, c;
        cin >> a >> b >> c;
        if (b > n || c > n || b < 1 || c < 1) {
            ans ++;
            continue;
        }
        if (a == 1) {
            if (indiff(b, c + n, q) || indiff(b, c + 2 * n, q)) {
                ans ++;
                continue;
            }
            uni(b, c, q);
            uni(b + n, c + n, q);
            uni(b + 2 * n, c + 2 * n, q);
            continue;
        }
        if (b == c) {
            ans ++;
            continue;
        }
        if (indiff(b, c, q) || indiff(b, c + 2 * n, q)) {
            ans ++;
            continue;
        }
        uni(b, c + n, q);
        uni(b + n, c + 2 * n, q);
        uni(b + 2 * n, c, q);
        continue;
    }
    cout << ans;
    return 0;
}

int find(int a, vector<int> &p) {
    if (a == p[a]) return a;
    return p[a] = find(p[a], p);
}

void uni(int a, int b, vector<int> &p){
    //hebing
    int rootb, roota;
    rootb = find(b, p);
    roota = find(a, p);
    p[rootb] = roota;
}

bool indiff(int a, int b, vector<int> &p) {
    return find(a, p) == find(b, p);
}
```

#### 题目9：

> ## 题目描述
>
> 给定一个长度为 n 的整数序列 ai，再给定一个长度为 n 的整数序列 bi。
>
> 你可以进行一些修改，每次你可以将一个 ai 增加 1，花费为 bi，你需要使所有的 ai 不相等，且同时满足花费最少。
>
> 但 zbw 认为太过简单，于是他规定，你可以在修改前进行**无限**次如下操作：交换 bi,bj(1≤i,j≤n)。
>
> 求最小的花费。
>
> **由于答案可能很大，请输出答案对  264 取模后的值。**
>
> ## 输入格式
>
> 第一行一个整数 n。
>
> 第二行 n 个整数，第 i 个数表示 ai。
>
> 第三行 n 个整数，第 i 个数表示 bi。
>
> ## 输出格式
>
> 输出一行一个整数，表示答案对 264 取模的值。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 3
> 3 3 3
> 1 2 3
> ```
>
> **输出 #1**
>
> ```
> 4
> ```
>
> **输入 #2**
>
> ```
> 3
> 3 3 4
> 3 2 1
> ```
>
> **输出 #2**
>
> ```
> 2
> ```
>
> **输入 #3**
>
> ```
> 3
> 3 4 5
> 2 1 3
> ```
>
> **输出 #3**
>
> ```
> 0
> ```
>
> ## 说明/提示
>
> 样例 1：不改变 b，让 a1 增加 2，a2 增加 1，总花费为 4。
>
> 样例 2：交换 b1,b3，让 a1 增加 2，总花费为 2。
>
> 样例 3：不做任何改变。
>
> **本题输入量较大，请使用读入优化。**
>
> | 测试点 |  n   |  ai  |   特殊性质   |
> | :----: | :--: | :--: | :----------: |
> |  1,2   | ≤10  | ≤1e9 |      无      |
> |  3∼6   | ≤1e3 | ≤1e9 |      无      |
> |  7∼10  | ≤1e6 | ≤1e6 |      无      |
> | 11∼14  | ≤1e6 | ≤1e9 | 所有 bi 相等 |
> | 15∼20  | ≤1e6 | ≤1e9 |      无      |
>
> 对于所有数据 1≤n≤1e6，1≤ai,bi≤1e9。

这题需要我们对重复数字进行转化（其实就是++），还不用管花费的对应关系。我们直接根据贪心先给他排序，再遍历重复的元素，创建一个栈或者队列来处理，最后解决即可。他甚至要我对2^64的进行取模，直接``unsigned long long``溢出不是就好了 ~~嘻嘻~~

那我们就先这样开始：

~~不嘻嘻了，搞不出来（~~ 搞出来了？真的假的？感谢高人相助！~~嘻嘻~~

我们维护一个栈，然后把下标推进去，如果没有相同的，再推出来，并使用当前位置的值减去那个下标，即是当前的增量。

```c++
	vector<int> count(1000005);
    int q = 0;
    unsigned long long ans = 0;
    stack<int> sta;


    for (int i = 1, j = a[1]; j <= a[n] || !sta.empty();) {
        while (a[i] == j) sta.push(i++);
        if (!sta.empty()) {
            if (j != a[sta.top()]) c[++ q] = j - a[sta.top()];
            sta.pop();
            ++ j;
        }
        else {
            j = a[i];
        }
    }
```

由此我们便完成了最难的部分，最后就是添加一个排序，让其最大 * 最小即可。

综上获得的代码如下：

```c++
#include <iostream>
#include <vector>
#include <algorithm>
#include <stack>

using namespace std;

int a[1000005], b[1000005], c[1000005];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n;
    cin >> n;

    for (int i = 1; i <= n; ++i) {
        cin >> a[i];
    }

    for (int i = 1; i <= n; ++i) {
        cin >> b[i];
    }

    sort(a + 1, a + 1 + n);
    sort(b + 1, b + 1 + n);

    vector<int> count(1000005);
    int q = 0;
    unsigned long long ans = 0;
    stack<int> sta;


    for (int i = 1, j = a[1]; j <= a[n] || !sta.empty();) {
        while (a[i] == j) sta.push(i++);
        if (!sta.empty()) {
            if (j != a[sta.top()]) c[++ q] = j - a[sta.top()];
            sta.pop();
            ++ j;
        }
        else {
            j = a[i];
        }
    }
    sort (c + 1, c + 1 + q, greater<int>());
    for (int i = 1; i <= q; ++i) {
        ans += static_cast<unsigned long long>(b[i]) * c[i] ;
    }
    cout << ans;
    return 0;
}
```

---

## 
