# CUGBACM25级暑假小学期训练 #10 图论基础

~~感觉最近好累，学不动了~~

## 一、建图

建图有好几种方式，以下是其中的几种。

### 1.1 vector 邻接表

一个带点数为\(n\)，边数为\(m\)的带权的图的模板如下：

```c++
const int N = 100005;
int n, m;
vector<pair<int,int> > e[N];
int main() {
	cin >> n >> m;
	for (int i = 1, u, v, w; i <= m; i++) {
		cin >> u >> v >> w;
		e[u].push_back({v, w});
	}
	return 0;
}
```

### 1.2 链状前向星

使用C++标准库自带的vector来模拟邻接表。虽然简便快捷，但在面临某些问题时却会稍显麻烦，典型的像删边、以及网络流问题里的建反边。

```c++
void add(int a, int b, int c) {
    to[++idx] = b;
    w[idx] = c;
    ne[idx] = head[a];
    head[a] = idx;
}
int main() {
    cin >> n >> m;
    for (int i = 1, u, v, c; i <= m; i++) {
        cin >> u >> v >> c;
        add(u,v,c);
    }
    return 0;
}
```

### 1.3 邻接矩阵

适合点数小、要随时查 u 到 v 有没有边。

#### 题目1：

> ### 题目
>
> 呃......变形课上Harry碰到了一点小麻烦,因为他并不像Hermione那样能够记住所有的咒语而随意的将一个棒球变成刺猬什么的,但是他发现了变形咒语的一个统一规律:如果咒语是以a开头b结尾的一个单词,那么它的作用就恰好是使A物体变成B物体. 
> Harry已经将他所会的所有咒语都列成了一个表,他想让你帮忙计算一下他是否能完成老师的作业,将一个B(ball)变成一个M(Mouse),你知道,如果他自己不能完成的话,他就只好向Hermione请教,并且被迫听一大堆好好学习的道理.
>
> ### Input
>
> 测试数据有多组。每组有多行，每行一个单词,仅包括小写字母,是Harry所会的所有咒语.数字0表示一组输入结束.
>
> ### Output
>
> 如果Harry可以完成他的作业,就输出"Yes.",否则就输出"No."(不要忽略了句号)

注：在vjudge中本题输入输出范例有问题，输入范例只给了一组（到是不是啥大问题），输出范例多了一行无效内容。

每个单词可视为一条路径，首字母 → 尾字母。询问能否借助路径，从字母``b``经过一系列传递后到达字母``m``。输入以``0``作为每组数据的结束标志。

本题我们只需要建立一个二维数组，或者二维向量，记录每个字母开头可以连接上的字母。之后调用BFS进行遍历。

代码如下：

```c++
#include <cstring>
#include <string>
#include <iostream>
#include <vector>
#include <queue>
using namespace std;

int vis[26];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    string w;
    while (cin >> w) {
        vector<int> g[30];
        int flag = 0;

        memset(vis, 0, sizeof(vis));

        while (w != "0") {
            flag = 1;
            int u = w.front() - 'a';
            int v = w.back() - 'a';
            g[u].push_back(v);
            if (!(cin >> w)) break;
        }
        if (!flag) continue;

        queue<int> q;

        vis[1] = 1;
        q.push(1);

        while (!q.empty()) {
            int u = q.front();
            q.pop();

            for (int v : g[u]) {
                if (!vis[v]) {
                    vis[v] = 1;
                    q.push(v);
                }
            }
        }

        cout << (vis[12] ? "Yes." : "No.") << '\n';
    }

    return 0;
}
```

