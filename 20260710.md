# CUGBACM25级暑假小学期训练 #5 字符串

## 一、字符串哈希

### 1.1 字符串哈希的特点

我们定义一个把字符串映射到整数的函数``𝑓``，这个``𝑓``称为是Hash函数。我们希望这个函数``𝑓``可以方便地帮我们判断两个字符串是否相等。这就是Hash，中文直接叫哈希。

Hash的核心思想在于，将输入映射到一个值域较小、可以方便比较的范围。

> [!NOTE]
>
> 这里的``值域较小``在不同情况下意义不同。
>
> 在哈希表中，值域需要小到能够接受线性的空间与时间复杂度。
>
> 在字符串哈希中，值域需要小到能够快速比较（10^9、10^18)都是可以快速比较的)。
>
> 同时，为了降低哈希冲突率，值域也不能太小。

以上内容来自于[OI Wiki](https://oi-wiki.org/string/hash/)。

字符串哈希的性质如下：

- 对于两个不同的哈希值，两个字符串一定是不同的。
- 对于两个相同的哈希值，两个字符串一般是相同的。

但是也存在哈希值相同，字符串不同。我们一般称此情况为哈希碰撞。

### 1.2 字符串哈希的映射

在拥有了这个工具，我们应当如何去计算？

一种映射方式是对于一个字符串，取每个字符的ASCII码，然后乘上base的次方，最后取模，得到一个哈希值。
$$
hash(X_1...X_n) = [X_1 × p^{n-1} +X_2 × p^{n-2} + ...+ X_n × p^{0} ] \: mod \: Q
$$

> [!TIP]
>
> 1、不能将字符映射为0，在此前提下，字符映射可以随意，此处为了方便直接采用字符的ASCII码。
>
> 2、根据经验，一般将设置P为131或 13331 , Q为1e9+7时，此时我们可以认为不产生哈希碰撞的冲突。
>
> 3、取模时，用 unsigned long long 类型来存储变量。
>
> 因为unsigned long long能够存储的最大值是2^64

既然如此，我们可以在这里应用前缀和，计算前缀哈希，并将其计算出来。

我们可以尝试着写出一个代码模板。

```c++
unsigned long long h[20];
const int mod = 1e9 + 7;

unsigned long long string_hash(string n) {
    int len = n.size();
    unsigned long long p = 131;
    // h[0] = 0;
    for (int i = 1; i <= n; ++i) {
        h[i] = (h[i - 1] * p + s[i - 1]) % mod;
    }
    return h[n];
}
```

#### 题目1：

> ## 题目描述
>
> 如题，给定 N 个字符串（第 i 个字符串长度为 Mi，字符串内包含数字、大小写字母，大小写敏感），请求出 N 个字符串中共有多少个不同的字符串。
>
> **友情提醒：如果真的想好好练习哈希的话，请自觉。**
>
> ## 输入格式
>
> 第一行包含一个整数 N，为字符串的个数。
>
> 接下来 N 行每行包含一个字符串，为所提供的字符串。
>
> ## 输出格式
>
> 输出包含一行，包含一个整数，为不同的字符串个数。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 5
> abc
> aaaa
> abc
> abcc
> 12345
> ```
>
> **输出 #1**
>
> ```
> 4
> ```
>
> ## 说明/提示
>
> ### 数据范围
>
> 对于 30% 的数据：N≤10，Mi≈6，Mmax≤15。
>
> 对于 70% 的数据：N≤1000，Mi≈100，Mmax≤150。
>
> 对于 100% 的数据：N≤10000，Mi≈1000，Mmax≤1500。
>
> ### 样例说明
>
> 样例中第一个字符串 abc 和第三个字符串 abc 是一样的，所以所提供字符串的集合为 {aaaa,abc,abcc,12345}，故共计 4 个不同的字符串。
>
> ### 拓展阅读
>
> 以下的一些试题从不同层面体现出了字符串哈希算法的正确性分析。
>
> - [P12197 Hash Killer I](https://www.luogu.com.cn/problem/P12197)
> - [P12198 Hash Killer II](https://www.luogu.com.cn/problem/P12198)
> - [P12199 （目前无解）Hash Killer III](https://www.luogu.com.cn/problem/P12199)
> - [P12200 Hash Killer Extra](https://www.luogu.com.cn/problem/P12200)
> - [P12201 Hash Killer Phantasm](https://www.luogu.com.cn/problem/P12201)
> - [P7350 「MCOI-04」Dream and Strings](https://www.luogu.com.cn/problem/P7350)

在本题中，我们直接套用上文的模板，然后将得到的unsigned long long的数导入set中，最后输出set的大小即可。

就这样，代码如下：

```c++
#include <iostream>
#include <string>
#include <set>
using namespace std;

unsigned long long h[1505];
const int q = 1e9 + 7;

unsigned long long str_hash(string s);

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n;
    cin >> n;
    string str;
    set<unsigned long long> ans;
    unsigned long long locans;
    for (int i = 0; i < n; ++i) {
        cin >> str;
        locans = str_hash(str);
        ans.insert(locans);
    }
    cout << ans.size();
}

unsigned long long str_hash(string s) {
    int n = s.size();
    int p = 131;
    for (int i = 1; i <= n; ++i) {
        h[i] = (h[i - 1] * p + s[i - 1]) % q;
    }
    return h[n];
}
```

#### 题目2：

> ## 题目描述
>
> 很久很久以前，森林里住着一群兔子。
>
> 有一天，兔子们想要研究自己的 DNA 序列。
>
> 我们首先选取一个好长好长的 DNA 序列（小兔子是外星生物，DNA 序列可能包含 26 个小写英文字母）。
>
> 然后我们每次选择两个区间，询问如果用两个区间里的 DNA 序列分别生产出来两只兔子，这两个兔子是否一模一样。
>
> 注意两个兔子一模一样只可能是他们的 DNA 序列一模一样。
>
> ## 输入格式
>
> 第一行输入一个 DNA 字符串 S。
>
> 第二行一个数字 m，表示 m 次询问。
>
> 接下来 m 行，每行四个数字 l1,r1,l2,r2，分别表示此次询问的两个区间，注意字符串的位置从 1 开始编号。
>
> ## 输出格式
>
> 对于每次询问，输出一行表示结果。
>
> 如果两只兔子完全相同输出 `Yes`，否则输出 `No`（注意大小写）。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> aabbaabb
> 3
> 1 3 5 7
> 1 3 6 8
> 1 2 1 2
> ```
>
> **输出 #1**
>
> ```
> Yes
> No
> Yes
> ```
>
> ## 说明/提示
>
> 数据保证，1≤∣S∣,m≤1e6。其中，∣S∣ 为字符串 S 的长度。

本题也是，判断序列是否相同。那我们就先输入这个字符串，然后再给他超级拆解再组装就好。

代码如下：

```c++
#include <iostream>
#include <string>
using namespace std;

unsigned long long h[1000005];
const int q = 1e9 + 7;

unsigned long long str_hash(string s, int begin, int end);

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    string str;
    cin >> str;

    int n;
    cin >> n;

    for (int i = 0; i < n; ++i) {
        int a, b, c, d;
        cin >> a >> b >> c >> d;
        if (str_hash(str, a, b) == str_hash(str, c, d))
            cout << "Yes" << '\n';
        else
            cout << "No" << '\n';
    }

    return 0;
}

unsigned long long str_hash(string s, int begin, int end) {
    int p = 131;
    h[begin] = 0;
    for (int i = begin + 1; i <= end; ++i) {
        h[i] = (h[i - 1] * p + s[i - 1]) % q;
    }
    return h[end];
}
```

不对，这玩意还是用前缀和算吧，直接暴力会TLE。

对于子串S[l,r]，其哈希值为：
$$
hash(l,r)=(h[r]−h[l−1]×pow[r−l+1]) \:mod \:q
$$
但是由于递推公式每次都乘以了一个``p``，我们需要再给他除回去。

``h[b] - 乘积`` 可能为负数。由于``h``和 ``pow``都是``unsigned long long``，减法会按无符号整数进行，负数会下溢成一个巨大的正数。所以我们要对其进行处理。先给乘积取模，再相减，再加q，使其仍为正，再取余数即可。

综上，代码如下：

```c++
#include <iostream>
#include <string>
using namespace std;

unsigned long long pow[1000005];
unsigned long long h[1000005];
const int q = 1e9 + 7;
const int p = 131;

unsigned long long str_hash(string s, int begin = 0, int end = 0);

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    string str;
    cin >> str;

    int n;
    cin >> n;
    str_hash(str);

    pow[0] = 1;
    for (int i = 1; i <= str.size(); ++i) {
        pow[i] = pow[i - 1] * p % q;
    }


    for (int i = 0; i < n; ++i) {
        int a, b, c, d;
        cin >> a >> b >> c >> d;
        if ((h[b] - (h[a - 1] * pow[b - a + 1]) % q + q) % q == (h[d] - (h[c - 1] * pow[d - c + 1]) % q + q) % q)
            cout << "Yes" << '\n';
        else
            cout << "No" << '\n';
    }

    return 0;
}

unsigned long long str_hash(string s, int begin, int end) {
    if (begin == end && begin == 0) {
        end = s.size();
    }
    h[begin] = 0;
    for (int i = begin + 1; i <= end; ++i) {
        h[i] = (h[i - 1] * p + s[i - 1]) % q;
    }
    return h[end];
}
```

#### 
