# CUGBACM25级暑假小学期训练 #6 大模拟&构造&思维

## 一、模拟

### 1.1 模拟

模拟就是用计算机来模拟题目中要求的操作。

模拟题目通常具有码量大、操作多、思路繁复的特点。由于它码量大，经常会出现难以查错的情况，如果在考试中写错是相当浪费时间的。

所以学长在培训过程中，对我们提出了一些建议。

> [!NOTE]
>
> 1. 先想好再开写，可以刻意约束自己：“先列出所有要用的变量，再开始写逻辑代码”
> 2. 总结并优化常用写法
> 3. 适度解耦和模块化
> 4. 谨慎使用多层分讨

#### 题目1：

> ## 题目背景
>
> 本题是高精度加法的模板题。
>
> ## 题目描述
>
> 给定两个非负整数 a,b，求它们的和。**不用考虑负数**。
>
> ## 输入格式
>
> 输入共两行，每行一个非负整数，分别为 a,b。
>
> ## 输出格式
>
> 输出一行一个非负整数，表示 a+b 的值。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 1
> 1
> ```
>
> **输出 #1**
>
> ```
> 2
> ```
>
> **输入 #2**
>
> ```
> 1001
> 9099
> ```
>
> **输出 #2**
>
> ```
> 10100
> ```
>
> ## 说明/提示
>
> 对于 20% 的测试数据，a,b≤10^9；
>  对于 40% 的测试数据，a,b≤10^18；
>  对于 100% 的测试数据，0≤a,b≤10^500。

本题我们可以使用模拟的方法来得出结果。

本题我使用了之前12月份的思路，原来是用C写的，用C++重新写了一次。

C代码如下：

```c++
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define MAX_LEN 100000

int strINTReverse(char *str, int *arr) {
    int len = strlen(str);
    for (int i = 0; i < len; i++) {
        arr[i] = str[len - 1 - i] - '0';
    }
    return len;
}//从正序到倒序

void gaojingjia(int *a, int a_len, int *b, int b_len, int *c) {
    int max_len;
    if (a_len >= b_len) {
        max_len = a_len;
    }
    else {
        max_len = b_len;
    }//计算长度
    int carry = 0;
    for (int i = 0; i < max_len; i++) {
        int sum = carry;
        if (i < a_len) sum += a[i];
        if (i < b_len) sum += b[i];
        c[i] = sum % 10;  //当前位
        carry = sum / 10;  //进位
    }
    if (carry > 0) {
        c[max_len] = carry;//最高位
    }
}

int main() {
    char d[MAX_LEN], e[MAX_LEN];
    int a[MAX_LEN] = {0}, b[MAX_LEN] = {0}, c[MAX_LEN + 1] = {0};

    gets(d);
    gets(e);
    int len1 = strINTReverse(d, a);
    int len2 = strINTReverse(e, b);

    gaojingjia(a, len1, b, len2, c);

    int result_len = (len1 > len2) ? len1 : len2;
    if (c[result_len] > 0) result_len++;  //有进位的情况

    for (int i = result_len - 1; i >= 0; i--) {
        printf("%d", c[i]);
    }
    return 0;
}
```

使用C++重写如下：

```c++
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int strToReverseVec(const string &str, vector<int> &arr) {
    arr.clear();
    for (auto it = str.rbegin(); it != str.rend(); ++it) {
        arr.push_back(*it - '0');
    }
    return arr.size();
}

void gaojingjia(const vector<int> &a, const vector<int> &b, vector<int> &c) {
    int max_len = max(a.size(), b.size());
    c.assign(max_len, 0);
    int carry = 0;
    for (int i = 0; i < max_len; ++i) {
        int sum = carry;
        if (i < (int)a.size()) sum += a[i];
        if (i < (int)b.size()) sum += b[i];
        c[i] = sum % 10;
        carry = sum / 10;
    }
    if (carry > 0) {
        c.push_back(carry);
    }
}

int main() {
    string d, e;
    getline(cin, d);
    getline(cin, e);

    vector<int> a, b, c;
    strToReverseVec(d, a);
    strToReverseVec(e, b);

    gaojingjia(a, b, c);

    for (auto it = c.rbegin(); it != c.rend(); ++it) {
        cout << *it;
    }
    cout << endl;

    return 0;
}
```



---

## 二、思维

### 2.1 无限问题

有些问题的操作是可以进行无限次的，我们需要找到其操作过程中的不变量进行处理。

#### 题目1：

> B. Odd Swap Sort
>
> time limit per test
>
> 1 second
>
> memory limit per test
>
> 256 megabytes
>
> You are given an array *a*1,*a*2,…,*a**n*. You can perform operations on the array. In each operation you can choose an integer *i* (1≤*i*<*n*), and swap elements *a**i* and *a**i*+1 of the array, if *a**i*+*a**i*+1 is odd.
>
> Determine whether it can be sorted in non-decreasing order using this operation any number of times.
>
> Input
>
> Each test contains multiple test cases. The first line contains a single integer *t* (1≤*t*≤105) — the number of test cases. Description of the test cases follows.
>
> The first line of each test case contains a single integer *n* (1≤*n*≤105) — the length of the array.
>
> The second line of each test case contains *n* integers *a*1,*a*2,…,*a**n* (1≤*a**i*≤109) — the elements of the array.
>
> It is guaranteed that the sum of *n* over all test cases does not exceed 2⋅105.
>
> Output
>
> For each test case, print "Yes" or "No" depending on whether you can or can not sort the given array.
>
> You may print each letter in any case (for example, "YES", "Yes", "yes", "yEs" will all be recognized as positive answer).
>
> Example
>
> Input
>
> Copy
>
> ```
> 4
> 4
> 1 6 31 14
> 2
> 4 2
> 5
> 2 9 6 7 10
> 3
> 6 6 6
> ```
>
> Output
>
> Copy
>
> ```
> Yes
> No
> No
> Yes
> ```
>
> Note
>
> In the first test case, we can simply swap 31 and 14 (31+14=45 which is odd) and obtain the non-decreasing array [1,6,14,31].
>
> In the second test case, the only way we could sort the array is by swapping 4 and 2, but this is impossible, since their sum 4+2=6 is even.
>
> In the third test case, there is no way to make the array non-decreasing.
>
> In the fourth test case, the array is already non-decreasing.

分析本题目，我们发现不变的是奇数序列顺序和偶数序列顺序，所以我们给他直接分别单独判断就好。

就酱紫。

代码如下：

```c++
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n;
    cin >> n;

    for (int i = 0; i < n; ++i) {
        int o;
        cin >> o;
        int tempa, tempb, tempc = -1, tempd = -1, flag = 1;
        for (int j = 0; j < o; ++j) {
            cin >> tempa;
            if (tempa % 2) {
                if (tempa >= tempd || tempd == -1) {
                    tempd = tempa;
                }
                else {
                    if (flag == 1){
                        cout << "No" << '\n';
                        flag --;
                        //break;
                    }
                }
            }
            else {
                if (tempa >= tempc || tempc == -1) {
                    tempc = tempa;
                }
                else {
                    if (flag == 1) {
                        cout << "No" << '\n';
                        flag --;
                        //break;
                    }
                }
            }
        }
        if (flag == 1) {
            cout << "Yes" << '\n';
        }

    }

    return 0;
}
```

