# CUGBACM25级暑假小学期训练 #12 博弈论

啊，时间真快啊，转眼就来到了这次小学期培训的最后一天。

今天是博弈论，但是好像第一题不是博弈论的东西捏（

#### 题目1：

> ## 题目背景
>
> 小 L 最近沉迷用谷歌生草机生草一些奇奇怪怪的东西。
>
> 小 L 在生草出了各式各样的作品后便开始考虑这样一个问题。
>
> ## 题目描述
>
> **注：为了方便描述，下文所有字符串起始位置都为 1，即都从 1 开始标号。**
>
> 小 L 将每次生草前的原文和生草后的结果看作两个**仅由小写字母组成**的两个字符串 A 和 B。
>
> 我们按如下方式定义「分割数列」和「分割串」：
>
> - 对于一个长度为 n 的字符串，定义它的一条「分割数列」为：存在长度为 k+2 的数列 p，使得 0=p0<p1<p2<...<pk<pk+1=n+1。对于一条「分割数列」，定义其「分割串」为 pi+1 到 pi+1−1 之间字符构成的子串（i∈[0,k]，可以为空串）。显然，对于一条长度为 k+2 的分割数列，一共有 k+1 个分割串。
> - 对于同一个字符串，两条分割数列（p 和 q）不同**当且仅当两条数列长度不同（k1\\=k2）**，或**存在 i 使得 pi\\=qi**。
>
> 不同人对于同样的原文和结果，他们的理解方式都是不同的。我们按如下方式定义一种理解方式：
>
> - 对于字符串 A 和 B，我们为这两个字符串各找一条分割数列（p 和 q），这两个分割数列满足以下要求：
>
> 1. 两个分割数列长度相等（k1=k2）。
> 2. 对于任意 i，有 A[pi]=B[qi]，即 **A 第 pi 个位置的字符与 B 第 qi 个位置的字符相同**。
>
> - 定义这种理解方式的「生草程度」为**此时两个字符串的所有分割串长度的平方和**，即 i=0∑k1(pi+1−pi−1)2+i=0∑k2(qi+1−qi−1)2。
> - 两种理解方式不同**当且仅当**两种理解方式的 p 不同，或两种理解方式的 q 不同。
>
> 小 L 想要知道所有理解方式的生草程度之和的结果。由于他不喜欢 109+7 这个数，他不希望你告诉他的结果会是这个数，所以你要将结果对 109+7 取模。
>
> ## 输入格式
>
> 第一行有两个正整数 n,m 分别为 A 和 B 的长度。
>
> 接下来一行有一个长度为 n 的字符串，表示字符串 A。
>
> 接下来一行有一个长度为 m 的字符串，表示字符串 B。
>
> ## 输出格式
>
> 一行，一个整数，表示答案对 109+7 取模的结果。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 3 4
> abc
> bacb
> ```
>
> **输出 #1**
>
> ```
> 74
> ```
>
> **输入 #2**
>
> ```
> 7 9
> adcbbde
> bdaegbcba
> ```
>
> **输出 #2**
>
> ```
> 2128
> ```
>
> ## 说明/提示
>
> 对于样例一，一共有以下理解方式：
>
> - p={0,4},q={0,5}，生草程度为 25。
> - p={0,1,4},q={0,2,5}，生草程度为 9。
> - p={0,2,4},q={0,1,5}，生草程度为 11。
> - p={0,2,4},q={0,4,5}，生草程度为 11。
> - p={0,3,4},q={0,3,5}，生草程度为 9。
> - p={0,1,2,4},q={0,2,4,5}，生草程度为 3。
> - p={0,1,3,4},q={0,2,3,5}，生草程度为 3。
> - p={0,2,3,4},q={0,1,3,5}，生草程度为 3。
>
> 总生草程度为 74。
>
> ### 数据范围
>
> **本题采用捆绑测试。**
>
> - Subtask 1( 20% )：n,m≤50。
> - Subtask 2( 30% )：n,m≤200。
> - Subtask 3( 50% )：无特殊限制。
>
> 对于 100% 的数据，n,m≤3000，A 和 B **仅包含小写字母**。

~~这题我先不弄了 没头绪~~ 我们来先进入今天的重点——博弈论。

## 一、博弈论

在算法竞赛和计算机科学中，我们主要研究公平组合博弈。它满足以下条件：

1. 有限：游戏在有限步内结束，且没有平局。
2. 完全信息：双方都知道当前局面和所有可能的操作。
3. 无随机性：不存在掷骰子等随机因素。
4. 无偏性（公平）：两名玩家能做的操作集合完全相同（只取决于局面，不取决于轮到谁）。

核心目标：给定一个初始局面，判断先手必胜还是先手必败（在双方都采用最优策略的前提下）。

> 关键术语：
>
> - P-position：必败态（Previous player winning），即轮到谁走谁输。
> - N-position：必胜态（Next player winning），即轮到谁走谁赢。

### 1.1 巴什博弈（Bash Game）—— 一堆石子

- **场景**：一堆 n 个石子，每次取1∼m个。
- **结论**：**\(n \% (m+1) == 0\)** 时，先手必败；否则先手必胜。
- **C++代码**：

```cpp
bool bashWin(int n, int m) {
    return n % (m + 1) != 0;
}
```

~~你看多么的人畜无害~~

#### 题目2：

> ## 题目背景
>
> Roy 和 October 两人在玩一个取石子的游戏。
>
> ## 题目描述
>
> 游戏规则是这样的：共有 n 个石子，两人每次都只能取 p^k 个（ p 为质数，k 为自然数，且 p^k 小于等于当前剩余石子数），谁取走最后一个石子，谁就赢了。
>
> 现在 October 先取，问她有没有必胜策略。
>
> 若她有必胜策略，输出一行 `October wins!`；否则输出一行 `Roy wins!`。
>
> ## 输入格式
>
> 第一行一个正整数 T，表示测试点组数。
>
> 第 2 行∼ 第 T+1 行，一行一个正整数 n，表示石子个数。
>
> ## 输出格式
>
> T 行，每行分别为 `October wins!` 或 `Roy wins!`。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 3
> 4
> 9
> 14
> ```
>
> **输出 #1**
>
> ```
> October wins!
> October wins!
> October wins!
> ```
>
> ## 说明/提示
>
> 对于 30% 的数据，1≤n≤30；
>
> 对于 60% 的数据，1≤n≤106；
>
> 对于 100% 的数据，1≤n≤5×107, 1≤T≤105。
>
> （改编题）

我们来一个个分析试试，首先是一堆石子，是巴什博奕。

石子==0的状态一定是必败态，因为对面在上一轮已经拿完了。

1~5个石子时，发现1=p^0,2=2^1,3=3^1,4=2^2,5=5^1，都是必胜态，可以一次拿完赢得游戏。

然后6个石子没办法一下拿完，因为6=p^k无自然数解。可以知道只能拿1~5个石子，这样都会转移到前面的必胜态，只不过这个必胜态已经是对面的了，所以说6个石子是你的必败态，在你面前出现6个石子又轮到你拿的时候，你必定失败。

所以只需要判断是否能被6整除就行。

代码如下：

```c++
#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, temp;
    cin >> n;
    for (int i = 0; i < n; ++i) {
        cin >> temp;
        if (temp % 6) cout << "October wins!" << '\n';
        else cout << "Roy wins!" << '\n';
    }

    return 0;
}
```

#### 题目3:

> ## 题目背景
>
> Roy 和 October 两人在玩另一个取石子的游戏。（第一季请看 P4018）
>
> ## 题目描述
>
> 游戏规则是这样的：共有 n 个石子，两人每次都只能取 pk 个（p 为质数，k=0 或 1，且 pk 小于等于当前剩余石子数），谁取走最后一个石子，谁就赢了。
>
> 现在 October 先取，问她有没有必胜策略。
>
> 若她有必胜策略，输出一行 `October wins!` ；否则输出一行 `Roy wins!`。
>
> ## 输入格式
>
> 第一行一个正整数 T，表示测试点组数。
>
> 第 2 行到第 T+1 行，一行一个正整数 n，表示石子个数。
>
> ## 输出格式
>
> T 行，每行分别为 `October wins!` 或 `Roy wins!`。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 3
> 5
> 7
> 14
> ```
>
> **输出 #1**
>
> ```
> October wins!
> October wins!
> October wins!
> ```
>
> ## 说明/提示
>
> 对于 30% 的数据，1≤n≤30；
>
> 对于 60% 的数据，1≤n≤106；
>
> 对于 100% 的数据，1≤n≤5×107，1≤T≤105。
>
> 花絮：由于出题人懒得造数据，就把 P4018 的输入直接拿来了 ovo

每次只能取1或一个质数，而质数模4只能是1,2,3。不可能是0。

- 若当前石子数是4的倍数，无论取1或质数，剩余石子数一定不是4的倍数。
- 若当前石子数模4余1，就取1个；余 2，就取2个；余3，就取3个。这样总能回到 4的倍数。

所以n mod 4 = 0 时先手必败。

代码如下：

```c++
#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, temp;
    cin >> n;
    for (int i = 0; i < n; ++i) {
        cin >> temp;
        if (temp % 4) cout << "October wins!" << '\n';
        else cout << "Roy wins!" << '\n';
    }

    return 0;
}
```

---

### 1.2 尼姆博弈（Nim Game）—— 多堆石子（最常用）

- **场景**：\(n\) 堆石子，每次选**一堆**取任意个。
- **结论**：所有堆数量的**异或和**为 0 则先手必败，否则必胜。
- **C++代码**：

```cpp
bool nimWin(vector<int>& piles) {
    int xor_sum = 0;
    for (int x : piles) xor_sum ^= x;
    return xor_sum != 0;
}
```

#### 题目4：

> ## 题目描述
>
> 甲，乙两个人玩 Nim 取石子游戏。
>
> Nim 游戏的规则是这样的：地上有 n 堆石子，每人每次可从任意一堆石子里取出任意多枚石子扔掉，可以取完，不能不取。每次只能从一堆里取。最后没石子可取的人就输了。假如甲是先手，且告诉你这 n 堆石子的数量，他想知道是否存在先手必胜的策略。
>
> ## 输入格式
>
> **本题有多组测试数据。**
>
> 第一行一个整数 T，表示有 T 组数据。
>
> 接下来每两行是一组数据：
>
> - 第一行一个整数 n，表示有 n 堆石子；
> - 第二行有 n 个非负整数，表示每一堆石子的数量。
>
> ## 输出格式
>
> 共 T 行，每行表示如果对于这组数据存在先手必胜策略则输出 `Yes`，否则输出 `No`。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 2
> 2
> 1 1
> 2
> 1 0
> ```
>
> **输出 #1**
>
> ```
> No
> Yes
> ```
>
> ## 说明/提示
>
> 对于所有数据，1≤T≤10，1≤n≤104，每堆石子的数量不超过 231−1。

这题直接套入模板，提交即可。

代码如下：

```c++
#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, temp;
    cin >> n;
    vector<int> vec;
    for (int i = 0; i < n; ++i) {
        cin >> temp;
        int xor_sum = 0;
        vec.clear();
        for (int j = 0; j < temp; ++j) {
            int tempa;
            cin >> tempa;
            vec.push_back(tempa);
        }
        for (int x : vec) xor_sum ^= x;
        if (xor_sum) cout << "Yes" << '\n';
        else cout << "No" << '\n';
    }

    return 0;
}
```

---

### 1.3 威佐夫博弈（Wythoff Game）—— 两堆石子

- **场景**：两堆石子，每次可以**拿一堆任意个**，或**两堆拿同样多**个。

- **结论**：设 a≤b，若

  $$
  (b-a) \times \frac{\sqrt{5}+1}{2} == a
  $$

  则先手必败（奇异局势），否则必胜。

- **C++代码**（注意精度）：

```cpp
bool wythoffWin(int a, int b) {
    if (a > b) swap(a, b);
    double r = (sqrt(5) + 1) / 2; // 黄金分割比
    int k = b - a;
    return (int)(k * r) != a; // 相等是必败，不等是必胜
}
```

#### 题目6：

> ## 题目描述
>
> 有两堆石子，数量任意，可以不同。游戏开始由两个人轮流取石子。游戏规定，每次有两种不同的取法：一是可以在任意的一堆中取走任意多的石子；二是可以在两堆中同时取走相同数量的石子。最后把石子全部取完者为胜者。现在给出初始的两堆石子的数目，你先取，假设双方都采取最好的策略，问最后你是胜者还是败者。
>
> ## 输入格式
>
> 输入共一行。
>
> 第一行共两个数 a,b，表示石子的初始情况。
>
> ## 输出格式
>
> 输出共一行。
>
> 第一行为一个数字 1,0 或 −1，如果最后你是胜利者则为 1；若失败则为 0；若结果无法确定则为 −1。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 8 4
> ```
>
> **输出 #1**
>
> ```
> 1
> ```
>
> ## 说明/提示
>
> **数据范围**
>
> 50% 的数据满足 a,b≤1000；
>
> 100% 的数据满足 a,b≤109。

这题直接套用上面的模板就好了。

注意下黄金分割率那玩意要用``long double``，还有下面那堆东西，不然会卡最后一个测试点。

代码如下：

```C++
#include <cmath>
#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    long double a, b;
    cin >> a >> b;

    if (a > b) swap(a, b);

    long double r = (sqrtl(5.0) + static_cast<long double>(1.0)) / static_cast<long double>(2.0);
    if ((int)((r) * (b - a)) != a) {
        cout << 1;
    }
    else cout << 0;

    return 0;
}
```

---

### 1.4 斐波那契博弈（Fibonacci Game）—— 取石子有倍数限制

- **场景**：一堆 \(n\)，先手不能全拿，之后每次拿的数量不超过上一个人的 **2倍**。
- **结论**：\(n\) 是**斐波那契数**时，先手必败；否则必胜。
- **C++代码**（预处理斐波那契数）：

```cpp
bool fibWin(int n) {
    set<int> fibSet;
    int a = 1, b = 2;
    while (a <= n) {
        fibSet.insert(a);
        int next = a + b;
        a = b;
        b = next;
    }
    return fibSet.find(n) == fibSet.end(); // 不是斐波那契数则必胜
}
```

---

### 1.5 阶梯博弈（Staircase Nim）—— 移到上一堆

- **场景**：\(n\) 级台阶，每级有石子。每次把第 \(i\) 级的任意石子移到 \(i-1\) 级，或取走第1级的石子。
- **结论**：**只看奇数级台阶**（第1、3、5...堆）的石子数，做**尼姆博弈**（异或和判断）。
- **C++代码**：

```cpp
bool stairNimWin(vector<int>& stairs) {
    int xor_sum = 0;
    for (int i = 0; i < stairs.size(); i++) {
        if (i % 2 == 0) { // 假设下标0代表第1级（奇数级）
            xor_sum ^= stairs[i];
        }
    }
    return xor_sum != 0;
}
```
