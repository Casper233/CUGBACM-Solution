注：上接[小学期训练#1 07-06](https://github.com/Casper233/CUGBACM-Solution/blob/main/20260706.md) 。

## 五、简单搜索

### 5.1 基础的搜索

基础的搜索很朴素，流程大体如下：

- 定义点集X为已经处理的点，点集F为未处理但是已经发现（也就是等待处理）的点。
- 初始化X为空，F只有搜索起点。
- 只要点集F不为空，循环以下内容。
  - 从点集F中取出一个点v。
  - 处理点v，并将点v放入点集X。
  - 遍历v的出边（如果是树就是子元素），对于其子元素，如果不在点集X和点集F中，将其加入点集F。
- 搜索结束。

我们能从中看到，其中``从点集F中取出一个点v``这一步没有定义，对于这一步如何进行实现，即为DFS与BFS。

如果实现的点集F为栈（先进后出），则为DFS。如果实现的点集F是队列（先进先出），则为BFS。

### 5.2 DFS深度优先搜索

深度优先搜索，简称DFS，Depth First Search，是一种用于遍历或搜索树或图的算法。所谓深度优先，就是说每次都尝试向更深的节点走。直到该节点无任何可以继续向更深的地方走为止。

该算法通常的时间复杂度为 O(n+m)。

我们看到他实现的方式有两种，可以通过循环，也可以通过递归。这两种方式有一些细微差异。

#### 5.2.1 栈实现

也就是通过上文的步骤完成。使用栈作为遍历中节点的暂存容器来实现；这与用队列实现的 BFS 形成高度对应。

```c++
vector<vector<int>> adj;  // 邻接表
vector<bool> vis;         // 记录节点是否已经遍历

void dfs(int s) {
  stack<int> st;
  st.push(s);
  vis[s] = true;

  while (!st.empty()) {
    int u = st.top();
    st.pop();

    for (int v : adj[u]) {
      if (!vis[v]) {
        vis[v] = true;  // 确保栈里没有重复元素
        st.push(v);
      }
    }
  }
}
```

#### 5.2.2 递归实现

通过递归进行的流程如下：

- 定义点集X为已经处理的点~~，点集F为未处理但是已经发现（也就是等待处理）的点~~。
- 初始化X为空~~，F只有搜索起点~~。
- ~~只要点集F不为空，循环以下内容。~~递归搜索点v（开始时是搜索起点）
  - ~~从点集F中取出一个点v。~~
  - 处理点v，并将点v放入点集X。
  - 遍历v的出边（如果是树就是子元素），对于其子元素，如果不在点集X~~和点集F中，将其加入点集F。~~，则递归处理该点
- 搜索结束。

代码实现如下：

```c++
#define N 1e5;
bool visited[N];
vector<int> children;

int dfs(int n) {
    visited[n] = true;
    do-something(n);
    for (auto i : children) {
        if (visited[i]) continue;
        dfs(i);
    }
}
```

#### 5.2.3 实现区别

如果有一个树的关系如下：

1 -> 2 -> 4

1 -> 3

且2与3同级。

如果使用的是用栈的dfs，则3会先被1发现，3被访问的路径是1 -> 3；

如果使用的是递归的dfs，则3会先被2发现，3被访问的路径是1 -> 2 -> 3。

就这样。

##### 题目1：

> ## 题目描述
>
> 已知 n 个整数 x1,x2,⋯,xn，以及 1 个整数 k（k<n）。从 n 个整数中任选 k 个整数相加，可分别得到一系列的和。例如当 n=4，k=3，4 个整数分别为 3,7,12,19 时，可得全部的组合与它们的和为：
>
> 3+7+12=22
>
> 3+7+19=29
>
> 7+12+19=38
>
> 3+12+19=34
>
> 现在，要求你计算出和为素数共有多少种。
>
> 例如上例，只有一种的和为素数：3+7+19=29。
>
> ## 输入格式
>
> 第一行两个空格隔开的整数 n,k（1≤n≤20，k<n）。
>
> 第二行 n 个整数，分别为 x1,x2,⋯,xn（1≤xi≤5×1e6）。
>
> ## 输出格式
>
> 输出一个整数，表示种类数。
>
> ## 输入输出样例
>
> **输入 #1**
>
> ```
> 4 3
> 3 7 12 19
> ```
>
> **输出 #1**
>
> ```
> 1
> ```
>
> ## 说明/提示
>
> **【题目来源】**
>
> NOIP 2002 普及组第二题

这题需要我们维护一个dfs的排序，我们可以在dfs过程中判断数量是否为k，然后加和是否为素数。

我们为了防止重复，选择规定dfs的方向为数列下标序号小到大。

代码如下：

```c++
#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

void dfs(int n, int sum, int num);

bool isprime(int n);

int cou, arr[30], ans, nu;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> nu >> cou;
    for (int i = 1; i <= nu; ++i) {
        cin >> arr[i];
    }
    dfs (1, 0, 1);
    cout << ans;
    return 0;
}

bool isprime(int n) {
    if (n <= 1) return false;
    for (int i = 2; i <= sqrt(n); ++i) {
        if (n % i == 0) return false;
    }
    return true;
}

void dfs(int n, int sum, int num) {
    if (n == cou + 1) {
        if (isprime(sum)) ans ++;
        return;
    }
    for (int i = num; i <= nu; ++i) {
        dfs (n + 1, sum + arr[i], i + 1);
    }
}
```
### 5.3 BFS广度优先搜索

广度优先搜索， Breadth First Search。是图论上最基础、最重要的搜索之一。他的排序是每次尝试同一层的内容，在完成该层访问后再访问下一层。

BFS可以实现从起点寻找到完成某任务的最短路径。即这条路径所包含的边数最小。

在BFS遍历的过程中，每个访问的内容都是从起点到该点最短距离访问的。

BFS的一种实现如下图所示。

```c++
void bfs(int u) {
  while (!Q.empty()) Q.pop();
  Q.push(u);
  vis[u] = 1;
  d[u] = 0;
  p[u] = -1;
  while (!Q.empty()) {
    u = Q.front();
    Q.pop();
    for (int i = head[u]; i; i = e[i].nxt) {
      if (!vis[e[i].to]) {
        Q.push(e[i].to);
        vis[e[i].to] = 1;
        d[e[i].to] = d[u] + 1;
        p[e[i].to] = u;
      }
    }
  }
}

void restore(int x) {
  vector<int> res;
  for (int v = x; v != -1; v = p[v]) {
    res.push_back(v);
  }
  std::reverse(res.begin(), res.end());
  for (int i = 0; i < res.size(); ++i) printf("%d", res[i]);
  puts("");
}
```

在其中，队列Q是记录要处理的节点，vis[]用于查看节点有没有被访问。

之后，我们每次从队列Q中取出队首的节点u，然后把与u相邻的所有节点v标记为已访问过并放入队列Q。

循环直至当队列 Q 为空，表示 BFS 结束。

在 BFS 的过程中，也可以记录一些额外的信息。例如上述代码中，d数组用于记录起点到某个节点的最短距离（要经过的最少边数），p数组记录是从哪个节点走到当前节点的。
